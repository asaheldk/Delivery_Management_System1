<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <meta name="theme-color" content="#071f31">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <title>장백수산 배송 통합 관리자 V2.5.3</title>
  <script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
  <script src="https://cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js"></script>
  <style>
    :root{--navy:#071f31;--navy2:#0c3c59;--teal:#07877c;--teal2:#12a593;--ink:#132c39;--muted:#667d88;--line:#d9e5e9;--bg:#edf4f6;--white:#fff;--green:#17814c;--amber:#b76b11;--red:#bd303a;--purple:#7455b8;--shadow:0 16px 48px rgba(5,38,56,.1)}
    *{box-sizing:border-box}html{min-width:320px;background:var(--bg);scroll-behavior:smooth}body{margin:0;color:var(--ink);background:radial-gradient(circle at 86% 0,rgba(7,135,124,.1),transparent 30%),var(--bg);font-family:-apple-system,BlinkMacSystemFont,"Apple SD Gothic Neo","Noto Sans KR",sans-serif;-webkit-font-smoothing:antialiased}button,input,select,textarea{font:inherit}button{cursor:pointer}button:disabled{opacity:.5;cursor:not-allowed}input,select,textarea{font-size:16px!important}.hidden{display:none!important}
    .login{min-height:100svh;display:grid;place-items:center;padding:max(24px,env(safe-area-inset-top)) 16px}.login-card{width:min(100%,450px);padding:28px;border:1px solid rgba(255,255,255,.8);border-radius:28px;background:rgba(255,255,255,.96);box-shadow:var(--shadow)}.mark{width:56px;height:56px;display:grid;place-items:center;border-radius:18px;color:#fff;background:linear-gradient(145deg,var(--navy2),var(--teal));font-size:26px}.login h1{margin:18px 0 5px;font-size:29px;letter-spacing:-.05em}.login p{margin:0 0 22px;color:var(--muted);line-height:1.6}.field{display:grid;gap:7px;margin-top:14px}.field span{font-size:13px;font-weight:850;color:#3d5966}.control{width:100%;min-height:48px;padding:10px 13px;border:1px solid #c8d8df;border-radius:13px;color:var(--ink);background:#fff;outline:0}.control:focus{border-color:var(--teal);box-shadow:0 0 0 4px rgba(7,135,124,.12)}textarea.control{min-height:92px;resize:vertical}.btn{min-height:44px;padding:0 15px;border:1px solid #cad9df;border-radius:12px;color:#284955;background:#fff;font-weight:850}.btn.primary{border:0;color:#fff;background:linear-gradient(135deg,#076f69,var(--teal2))}.btn.danger{color:var(--red);border-color:#efb9bd;background:#fff5f5}.btn.warn{color:#8d550b;border-color:#eed2a4;background:#fff9ed}.btn.purple{color:#5c4291;border-color:#d8cbed;background:#f8f4ff}.btn.full{width:100%;margin-top:18px}.message{min-height:21px;margin-top:11px;color:var(--red);font-size:14px;font-weight:800}
    .top{position:sticky;top:0;z-index:30;color:#fff;background:linear-gradient(120deg,var(--navy),var(--navy2) 66%,#07635f);box-shadow:0 9px 28px rgba(4,30,46,.2)}.top-main{min-height:70px;display:flex;align-items:center;justify-content:space-between;gap:14px;padding:max(10px,env(safe-area-inset-top)) max(20px,env(safe-area-inset-right)) 10px max(20px,env(safe-area-inset-left))}.brand{display:flex;align-items:center;gap:11px}.brand-logo{width:43px;height:43px;display:grid;place-items:center;border-radius:14px;background:rgba(255,255,255,.12)}.brand strong,.brand small{display:block}.brand small{margin-top:2px;color:#b9d7df;font-size:11px}.account{display:flex;align-items:center;gap:9px}.account small,.account strong{display:block;text-align:right}.account small{color:#bad6df;font-size:11px}.top-button{min-height:39px;padding:0 11px;border:1px solid rgba(255,255,255,.24);border-radius:10px;color:#fff;background:rgba(255,255,255,.08);font-size:13px;font-weight:850}.refresh-button{border-color:rgba(131,235,219,.42);background:rgba(13,147,132,.24)}.tabs{display:flex;gap:5px;padding:0 20px 10px;overflow-x:auto;scrollbar-width:none}.tabs::-webkit-scrollbar{display:none}.tab{flex:0 0 auto;min-height:39px;padding:0 13px;border:0;border-radius:11px;color:#c7dce3;background:transparent;font-size:13px;font-weight:850}.tab.active{color:#083444;background:#fff}.tab .count{display:inline-grid;min-width:20px;height:20px;place-items:center;margin-left:4px;border-radius:99px;color:#fff;background:var(--red);font-size:11px}
    .wrap{width:min(1540px,100%);margin:auto;padding:22px 22px calc(34px + env(safe-area-inset-bottom))}.page-head{display:flex;align-items:end;justify-content:space-between;gap:14px;margin-bottom:16px}.page-head h1{margin:0;font-size:clamp(25px,4vw,34px);letter-spacing:-.05em}.page-head p{margin:5px 0 0;color:var(--muted)}.actions{display:flex;align-items:center;gap:8px}.date{width:150px}.summary{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:10px}.metric{padding:16px;border:1px solid rgba(210,225,231,.9);border-radius:18px;background:rgba(255,255,255,.94);box-shadow:0 8px 24px rgba(9,43,64,.05)}.metric small,.metric strong{display:block}.metric small{color:var(--muted);font-weight:780}.metric strong{margin-top:4px;font-size:28px;letter-spacing:-.04em}.metric i{display:block;width:28px;height:4px;margin-top:9px;border-radius:99px;background:var(--teal)}.metric.bad i{background:var(--red)}.metric.warn i{background:var(--amber)}
    .panel{margin-top:14px;border:1px solid var(--line);border-radius:21px;background:rgba(255,255,255,.95);box-shadow:0 10px 32px rgba(9,43,64,.055);overflow:hidden}.panel-head{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:16px 17px;border-bottom:1px solid var(--line)}.panel-head h2{margin:0;font-size:19px}.panel-head p{margin:4px 0 0;color:var(--muted);font-size:13px}.filters{display:flex;gap:7px}.search{width:220px}.table-scroll{overflow-x:auto}.table{width:100%;border-collapse:collapse}.table th{padding:10px 13px;color:#5c737e;background:#f5f9fa;font-size:12px;text-align:left;white-space:nowrap}.table td{padding:13px;border-top:1px solid #e7eef1;vertical-align:middle}.table tbody tr:hover{background:#f8fbfc}.table small{color:var(--muted)}.name{display:flex;align-items:center;gap:9px}.avatar{width:38px;height:38px;flex:0 0 auto;display:grid;place-items:center;border-radius:12px;color:#fff;background:linear-gradient(135deg,#0a5770,#0a9884);font-weight:900}.name strong,.name small{display:block}.vehicle{display:inline-grid;min-width:50px;min-height:32px;place-items:center;border-radius:10px;color:#08635d;background:#e4f7f3;font-weight:900}.badge{display:inline-flex;align-items:center;min-height:28px;padding:0 9px;border-radius:99px;color:#536c77;background:#edf3f5;font-size:12px;font-weight:850}.badge.green{color:#12693e;background:#e8f8ef}.badge.red{color:#a72932;background:#fff0f1}.badge.amber{color:#8e570d;background:#fff4df}.badge.purple{color:#604695;background:#f2edff}.row-actions{display:flex;justify-content:flex-end;gap:6px}.mini{min-height:36px;padding:0 10px;border:1px solid #d2dfe4;border-radius:10px;color:#294b58;background:#fff;font-size:12px;font-weight:850}.mobile-cards{display:none}.empty{padding:44px 18px;color:var(--muted);text-align:center}.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:14px}.board{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;padding:14px}.board-card{padding:14px;border:1px solid var(--line);border-radius:16px;background:#fff}.board-card.ready{border-color:#9fdcc8;background:#f1fbf7}.board-card.approved{border-color:#69bea0;background:#e8f8f1}.board-top{display:flex;justify-content:space-between;gap:8px}.board-card h3{margin:0;font-size:16px}.checks{display:flex;flex-wrap:wrap;gap:5px;margin:10px 0}.check{padding:5px 7px;border-radius:7px;color:#7b6264;background:#fff0f1;font-size:11px;font-weight:800}.check.ok{color:#176940;background:#e8f7ef}.board-actions{display:grid;grid-template-columns:1fr 1fr;gap:6px}.health{display:grid;grid-template-columns:repeat(3,1fr);gap:9px;padding:14px}.health-card{padding:14px;border-radius:15px;background:#f1f6f7}.health-card small,.health-card strong{display:block}.health-card strong{margin-top:4px;font-size:20px}.close-banner{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:14px;padding:14px 16px;border-radius:16px;color:#6c4a0a;background:#fff3d7}.close-banner.closed{color:#145f3b;background:#e8f8ef}
    .card-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:11px;padding:14px}.info-card{padding:15px;border:1px solid var(--line);border-radius:16px;background:#fff}.info-card h3{margin:0 0 5px;font-size:17px}.info-card p{margin:4px 0;color:var(--muted);font-size:13px;line-height:1.5}.info-card .tags{display:flex;flex-wrap:wrap;gap:5px;margin:10px 0}.info-card .row-actions{margin-top:12px}.urgent-card{border-left:5px solid var(--red)}.incident-card{border-left:5px solid var(--amber)}.product-price{font-size:20px;font-weight:900}.money{font-variant-numeric:tabular-nums;font-weight:850}.role-matrix{display:grid;grid-template-columns:repeat(5,1fr);gap:9px;padding:14px}.role-card{padding:13px;border:1px solid var(--line);border-radius:15px}.role-card h3{margin:0;font-size:15px}.role-card p{min-height:50px;margin:6px 0;color:var(--muted);font-size:12px;line-height:1.5}.audit-detail{max-width:420px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.menu-badges{display:flex;flex-wrap:wrap;gap:4px}.permission-box{grid-column:1/-1;margin-top:14px;padding:15px;border:1px solid #c9dde3;border-radius:17px;background:#f7fbfc}.permission-head{display:flex;align-items:start;justify-content:space-between;gap:12px}.permission-head strong{display:block;font-size:16px}.permission-head p{margin:5px 0 0;color:var(--muted);font-size:13px;line-height:1.5}.permission-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:13px}.menu-option{position:relative;display:grid;grid-template-columns:24px 1fr auto;align-items:center;gap:9px;min-height:68px;padding:10px;border:1px solid #d8e5e9;border-radius:13px;background:#fff;cursor:pointer}.menu-option:has(input:checked){border-color:#58b5a8;background:#effaf7;box-shadow:0 0 0 2px rgba(7,135,124,.07)}.menu-option input{width:21px;height:21px;margin:0;accent-color:var(--teal)}.menu-option strong,.menu-option small{display:block}.menu-option strong{font-size:14px}.menu-option small{margin-top:3px;color:var(--muted);font-size:11px;line-height:1.35}.recommend-mark{padding:4px 6px;border-radius:7px;color:#5b438c;background:#f1ebff;font-size:10px;font-style:normal;font-weight:900}.permission-tools{display:flex;flex-wrap:wrap;gap:7px;margin-top:11px}.permission-hint{margin:10px 0 0;color:#5f7580;font-size:12px;line-height:1.55}
    .modal{position:fixed;inset:0;z-index:80;display:grid;place-items:center;padding:15px;background:rgba(3,21,31,.74);backdrop-filter:blur(7px)}.modal-card{width:min(100%,650px);max-height:calc(100dvh - 26px);overflow-y:auto;border-radius:24px;background:#fff;box-shadow:0 30px 90px rgba(0,0,0,.28)}.modal-card.wide{width:min(100%,1100px)}.modal-head{position:sticky;top:0;z-index:2;display:flex;align-items:start;justify-content:space-between;gap:12px;padding:17px;border-bottom:1px solid var(--line);background:rgba(255,255,255,.96)}.modal-head h2{margin:0;font-size:21px}.modal-head p{margin:4px 0 0;color:var(--muted);font-size:13px}.close{width:40px;height:40px;border:0;border-radius:12px;background:#eef3f5;font-size:21px}.modal-body{padding:17px}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:0 11px}.wide-field{grid-column:1/-1}.form-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:18px}.notice{margin-top:12px;padding:11px 12px;border-radius:12px;color:#76510c;background:#fff5dc;font-size:13px;line-height:1.5}.toast{position:fixed;right:17px;bottom:17px;z-index:100;max-width:370px;padding:13px 15px;border-radius:13px;color:#fff;background:#126d48;box-shadow:0 16px 42px rgba(0,0,0,.24);font-weight:850}.toast.bad{background:var(--red)}
    .workday-toolbar{display:flex;justify-content:flex-end;gap:8px;margin:0 0 13px}.delivery-detail{margin-top:11px;border:1px solid var(--line);border-radius:17px;overflow:hidden;background:#fff}.delivery-detail.cancelled{opacity:.66}.delivery-detail-head{display:flex;align-items:start;justify-content:space-between;gap:10px;padding:14px;background:#f6fafb}.delivery-detail-head h3{margin:0;font-size:17px}.delivery-detail-head p{margin:4px 0 0;color:var(--muted);font-size:13px}.detail-section{padding:13px 14px;border-top:1px solid #e6eef1}.detail-section h4{margin:0 0 9px;font-size:14px}.detail-line{display:grid;grid-template-columns:minmax(130px,1.25fr) repeat(4,minmax(85px,1fr));gap:7px;padding:9px 0;border-top:1px dashed #dbe6ea;align-items:center;font-size:12px}.detail-line:first-of-type{border-top:0}.detail-line strong,.detail-line span{min-width:0;overflow-wrap:anywhere}.weigh-chips{display:flex;flex-wrap:wrap;gap:4px;margin-top:6px}.weigh-chips span{padding:4px 6px;border-radius:7px;color:#35545f;background:#eef4f6;font-size:11px}.audit-list{display:grid;gap:7px}.audit-row{padding:10px;border:1px solid var(--line);border-radius:12px;background:#f8fbfc;font-size:12px}.editor-delivery{margin-top:13px;padding:13px;border:1px solid #cddde3;border-radius:17px;background:#f8fbfc}.editor-delivery-head{display:flex;align-items:center;justify-content:space-between;gap:9px;margin-bottom:9px}.editor-delivery-head h3{margin:0;font-size:17px}.editor-items,.editor-boxes{display:grid;gap:9px;margin-top:10px}.editor-item,.editor-box{padding:11px;border:1px solid #dce7eb;border-radius:14px;background:#fff}.editor-item-head{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:6px}.editor-item-head strong{font-size:14px}.compact-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:0 8px}.check-field{display:flex;align-items:center;gap:8px;min-height:48px;margin-top:14px;padding:10px 12px;border:1px solid #d4e1e6;border-radius:13px;background:#fff;font-size:13px;font-weight:800}.check-field input{width:21px;height:21px;accent-color:var(--teal)}.subhead{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-top:13px}.subhead h4{margin:0}.completion-editor{margin-top:14px;padding:13px;border:1px solid #c8ded8;border-radius:16px;background:#f2faf8}
    @media(max-width:1180px){.summary{grid-template-columns:repeat(3,1fr)}.board{grid-template-columns:repeat(3,1fr)}.card-grid{grid-template-columns:repeat(2,1fr)}.role-matrix{grid-template-columns:repeat(3,1fr)}}
    @media(max-width:760px){.top-main{padding-left:13px;padding-right:13px}.account-copy{display:none}.refresh-label{display:none}.refresh-button{width:42px;padding:0;font-size:19px}.tabs{padding-left:10px;padding-right:10px}.wrap{padding:16px 11px calc(28px + env(safe-area-inset-bottom))}.page-head{align-items:stretch;flex-direction:column}.actions{display:grid;grid-template-columns:1fr 1fr}.date{width:100%}.summary{grid-template-columns:repeat(2,1fr);gap:8px}.metric{padding:14px}.metric strong{font-size:25px}.panel-head{align-items:stretch;flex-direction:column}.filters{display:grid;grid-template-columns:1fr 120px}.search{width:100%}.table-scroll{display:none}.mobile-cards{display:grid;gap:9px;padding:10px}.mobile-card{padding:14px;border:1px solid var(--line);border-radius:16px;background:#fff}.mobile-card .row-actions{display:grid;grid-template-columns:1fr 1fr;margin-top:11px}.board{grid-template-columns:1fr 1fr;padding:10px}.grid-2{grid-template-columns:1fr}.card-grid{grid-template-columns:1fr;padding:10px}.health{grid-template-columns:1fr 1fr;padding:10px}.role-matrix{grid-template-columns:1fr 1fr;padding:10px}.modal{align-items:end;padding:max(7px,env(safe-area-inset-top)) 0 0}.modal-card,.modal-card.wide{width:100%;max-height:calc(100dvh - max(7px,env(safe-area-inset-top)));border-radius:23px 23px 0 0}.form-grid,.compact-grid{grid-template-columns:1fr}.wide-field,.permission-box{grid-column:auto}.permission-grid{grid-template-columns:1fr}.permission-head{align-items:stretch;flex-direction:column}.permission-head .btn{width:100%}.detail-line{grid-template-columns:1fr 1fr}.detail-line strong{grid-column:1/-1}.workday-toolbar{display:grid;grid-template-columns:1fr}.workday-toolbar .btn{width:100%}}
    @media(max-width:430px){.brand small{display:none}.board{grid-template-columns:1fr}.role-matrix{grid-template-columns:1fr}.health{grid-template-columns:1fr}.form-actions{grid-template-columns:1fr}.close-banner{align-items:stretch;flex-direction:column}}
    @media print{.top,.no-print,.toast,.modal{display:none!important}.wrap{width:100%;padding:0}.panel{box-shadow:none}.table-scroll{display:block}.table{font-size:11px}}
  </style>
</head>
<body>
  <section id="loginView" class="login">
    <form id="loginForm" class="login-card">
      <div class="mark">管</div><h1>배송 통합 관리자 V2.5.3</h1><p>전체 배송원의 출차·거래처·긴급 발주·품목·회계·권한을 한곳에서 관리합니다.</p>
      <label class="field"><span>관리자 이메일</span><input class="control" name="email" type="email" autocomplete="username" required></label>
      <label class="field"><span>비밀번호</span><input class="control" name="password" type="password" autocomplete="current-password" required></label>
      <button class="btn primary full" type="submit">관리자 로그인</button><div id="loginMessage" class="message"></div>
    </form>
  </section>
  <div id="app" class="hidden">
    <header class="top">
      <div class="top-main"><div class="brand"><span class="brand-logo">🚚</span><div><strong>장백수산 배송 관제</strong><small>OPERATIONS CENTER V2.5.3</small></div></div><div class="account"><div class="account-copy"><strong id="adminName">관리자</strong><small id="adminRole"></small></div><button class="top-button refresh-button" data-action="top-refresh" type="button" title="최신 배포 파일 다시 불러오기">↻ <span class="refresh-label">업데이트 새로고침</span></button><button id="logoutButton" class="top-button" type="button">로그아웃</button></div></div>
      <nav id="tabs" class="tabs"></nav>
    </header>
    <main id="content" class="wrap"></main>
  </div>
  <div id="modal" class="modal hidden"></div><div id="toast" class="toast hidden"></div>
  <script>
  (()=>{"use strict";
    const $=s=>document.querySelector(s), esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"})[c]);
    const localDate=(o=0)=>{const d=new Date();d.setDate(d.getDate()+o);return new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,10)},money=n=>new Intl.NumberFormat("ko-KR").format(Math.round(Number(n)||0)),fmt=v=>v?new Date(v).toLocaleString("ko-KR"):"-";
    const ADMIN_ROLES=["admin","super_admin","dispatch_manager","accounting_manager","field_manager","viewer"];
    const roleLabel={admin:"전체 관리자(기존)",super_admin:"최고 관리자",dispatch_manager:"배차 관리자",accounting_manager:"회계 관리자",field_manager:"현장 관리자",viewer:"조회 전용",driver:"배송원"};
    const rolePerm={admin:["*"],super_admin:["*"],dispatch_manager:["driver_write","account_manage","dispatch_write","store_write","store_secret","urgent_write","incident_write","departure_write","report_read"],accounting_manager:["product_write","accounting_write","report_read"],field_manager:["store_write","urgent_write","incident_write","departure_write","report_read"],viewer:["report_read"]};
    const tabs=[
      ["dashboard","오늘 관제","report_read"],["shipments","출고량 분석","report_read"],["purchases","매입전표","report_read"],["drivers","배송원·출차","driver_write"],["stores","거래처·인수인계","store_write"],["urgent","긴급·오류","urgent_write"],["products","품목·가격","product_write"],["accounting","회계·보고","report_read"],["access","권한·감사","role_manage"]
    ];
    const menuDescription={dashboard:"당일 업무·출차 현황",shipments:"제품별·호차별 출고량",purchases:"호차별 매입전표·구매 합계",drivers:"배송원·호차·출차 승인",stores:"거래처·배송 인수인계",urgent:"긴급 발주·오류 조치",products:"품목·단가 중앙 관리",accounting:"거래명세·미수·보고서",access:"관리자 계정·감사 기록"};
    const roleMenuDefaults={
      admin:tabs.map(x=>x[0]),
      super_admin:tabs.map(x=>x[0]),
      dispatch_manager:["dashboard","shipments","purchases","drivers","stores","urgent"],
      accounting_manager:["dashboard","shipments","purchases","products","accounting"],
      field_manager:["dashboard","shipments","purchases","drivers","stores","urgent"],
      viewer:["dashboard","shipments","purchases","drivers","stores","urgent","products","accounting"]
    };
    let client,session,me,view="dashboard",workDate=localDate(1),search="",shipmentVehicle="all",shipmentStatus="all",poll;
    let data={profiles:[],statuses:[],snapshots:[],assignments:[],workday:null,departures:[],stores:[],urgent:[],incidents:[],products:[],prices:[],invoices:[],receivables:[],logs:[]};
    const can=p=>p==="report_read"||rolePerm[me?.role]?.includes("*")||rolePerm[me?.role]?.includes(p);
    const menuIdsFor=p=>Array.isArray(p?.menu_permissions)?p.menu_permissions:(roleMenuDefaults[p?.role]||["dashboard"]);
    const menuLabel=id=>tabs.find(x=>x[0]===id)?.[1]||id;
    function toast(msg,bad=false){const e=$("#toast");e.textContent=msg;e.classList.toggle("bad",bad);e.classList.remove("hidden");clearTimeout(toast.t);toast.t=setTimeout(()=>e.classList.add("hidden"),3300)}
    async function forceRefresh(button){if(button){button.disabled=true;button.innerHTML='↻ <span class="refresh-label">최신 파일 확인 중</span>'}try{if("caches"in window){const keys=await caches.keys();await Promise.all(keys.map(key=>caches.delete(key)))}if("serviceWorker"in navigator){const registrations=await navigator.serviceWorker.getRegistrations();await Promise.all(registrations.map(registration=>registration.update().catch(()=>null)))}const url=new URL(location.href);url.searchParams.set("update",Date.now());url.hash="";location.replace(url.toString())}catch{location.reload()}}
    function online(s){return s?.last_sync_at&&Date.now()-new Date(s.last_sync_at).getTime()<130000}function statusOf(id){return data.statuses.find(x=>x.driver_id===id)||{work_state:"not_started",delivery_total:0,delivery_completed:0,tank_total:0,tank_loaded:0,box_total:0,box_checked:0,urgent_pending:0,issue_count:0}}function completionOf(id){const snapshot=data.snapshots.find(x=>x.driver_id===id);return snapshot?.payload?.workClosures?.[workDate]||null}
    function stateLabel(v){return({not_started:"미시작",working:"출차 준비",departure_ready:"점검 완료",delivering:"배송 중",completed:"배송 완료"})[v]||"미시작"}function roleBadge(r){return `<span class="badge ${r==="super_admin"||r==="admin"?"purple":""}">${esc(roleLabel[r]||r)}</span>`}
    async function getConfig(){try{const r=await fetch("/api/config",{cache:"no-store"});return r.ok?await r.json():{configured:false}}catch{return{configured:false}}}
    async function api(path,body){const r=await fetch(path,{method:"POST",headers:{"Content-Type":"application/json","Authorization":`Bearer ${session.access_token}`},body:JSON.stringify(body)}),j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.error||"요청을 처리하지 못했습니다.");return j}
    async function boot(){const cfg=await getConfig();if(!cfg.configured||!window.supabase?.createClient){$("#loginMessage").textContent="Vercel 환경변수와 Supabase 설정을 확인해 주세요.";return}client=window.supabase.createClient(cfg.supabaseUrl,cfg.supabaseAnonKey,{auth:{persistSession:true,autoRefreshToken:true}});const {data:s}=await client.auth.getSession();if(s.session)await accept(s.session).catch(e=>$("#loginMessage").textContent=e.message)}
    async function accept(s){session=s;const {data:p,error}=await client.from("profiles").select("*").eq("id",s.user.id).maybeSingle();if(error||!p?.active||!ADMIN_ROLES.includes(p.role)){await client.auth.signOut();throw new Error("관리자 권한이 없는 계정입니다.")}me=p;$("#loginView").classList.add("hidden");$("#app").classList.remove("hidden");$("#adminName").textContent=p.full_name;$("#adminRole").textContent=roleLabel[p.role];renderTabs();await loadAll();clearInterval(poll);poll=setInterval(loadAll,30000)}
    function visibleTabs(){const allowed=menuIdsFor(me);return tabs.filter(([id])=>allowed.includes(id)&&(id!=="access"||can("role_manage")))}
    function renderTabs(){const available=visibleTabs();if(!available.some(x=>x[0]===view))view=available[0]?.[0]||"dashboard";$("#tabs").innerHTML=available.map(([id,label])=>`<button class="tab ${id===view?"active":""}" data-view="${id}" type="button">${label}${id==="urgent"&&data.urgent.filter(x=>["new","acknowledged","loaded"].includes(x.status)).length?`<span class="count">${data.urgent.filter(x=>["new","acknowledged","loaded"].includes(x.status)).length}</span>`:""}</button>`).join("")}
    async function query(table,select="*",builder=x=>x){const r=await builder(client.from(table).select(select));if(r.error){console.warn(table,r.error.message);return[]}return r.data||[]}
    async function loadAll(){if(!client||!session)return;const d=workDate;
      const results=await Promise.all([
        query("profiles"),query("driver_daily_status","*",q=>q.eq("work_date",d)),query("driver_state_snapshots","driver_id,work_date,payload,updated_at",q=>q.eq("work_date",d)),query("vehicle_assignments","*",q=>q.order("created_at",{ascending:false}).limit(500)),query("workday_controls","*",q=>q.eq("work_date",d)),query("departure_checks","*",q=>q.eq("work_date",d)),query("stores","id,store_code,name,address,phone,contact_name,assigned_driver_id,assigned_vehicle,delivery_order,preferred_time,entry_method,placement,prohibited,parking_note,handover_note,photo_urls,active,updated_by,created_at,updated_at",q=>q.order("assigned_vehicle").order("delivery_order")),query("urgent_orders","*",q=>q.eq("work_date",d).order("created_at",{ascending:false})),query("incident_reports","*",q=>q.eq("work_date",d).order("created_at",{ascending:false})),query("products","*",q=>q.order("category").order("name")),query("product_prices","*",q=>q.order("effective_from",{ascending:false})),query("invoice_records","*",q=>q.eq("work_date",d).order("created_at",{ascending:false})),query("receivable_payments","*",q=>q.eq("work_date",d).order("created_at",{ascending:false})),query("activity_logs","*",q=>q.order("created_at",{ascending:false}).limit(300))
      ]);
      [data.profiles,data.statuses,data.snapshots,data.assignments,,,,data.urgent,data.incidents,data.products,data.prices,data.invoices,data.receivables,data.logs]=results;data.workday=results[4][0]||null;data.departures=results[5];data.stores=results[6];renderTabs();render()}
    function pageHead(title,desc,buttons=""){return `<section class="page-head"><div><h1>${title}</h1><p>${desc}</p></div><div class="actions no-print"><input id="workDate" class="control date" type="date" value="${workDate}">${buttons}</div></section>`}
    function metrics(items){return `<section class="summary">${items.map(([a,b,c=""])=>`<article class="metric ${c}"><small>${a}</small><strong>${b}</strong><i></i></article>`).join("")}</section>`}
    function panel(title,desc,body,tools=""){return `<section class="panel"><header class="panel-head"><div><h2>${title}</h2><p>${desc}</p></div>${tools}</header>${body}</section>`}
    function render(){({dashboard:renderDashboard,shipments:renderShipments,purchases:renderPurchases,drivers:renderDrivers,stores:renderStores,urgent:renderUrgent,products:renderProducts,accounting:renderAccounting,access:renderAccess}[view]||renderDashboard)();if(view==="shipments")$("#content").innerHTML=$("#content").innerHTML.replaceAll("납품 완료만","전체 배송 완료 호차").replaceAll("준비·배송 중","전체 배송 진행 중")}
    const dateKey=value=>String(value||"").slice(0,10);
    function drivers(){return data.profiles.filter(x=>x.role==="driver").sort((a,b)=>(a.vehicle_number||999)-(b.vehicle_number||999))}
    function snapshotForDriver(id){return data.snapshots.find(row=>row.driver_id===id)||null}
    function datedDeliveries(id,date=workDate,includeCancelled=true){const snapshot=snapshotForDriver(id),rows=Array.isArray(snapshot?.payload?.deliveries)?snapshot.payload.deliveries:[];return rows.filter(row=>dateKey(row.deliveryDate)===dateKey(date)&&(includeCancelled||row.status!=="cancelled")).sort((a,b)=>(Number(a.routeOrder)||999)-(Number(b.routeOrder)||999))}
    function hasWorkForDate(driver){const deliveries=datedDeliveries(driver.id,workDate,false),urgent=data.urgent.some(row=>row.assigned_driver_id===driver.id||Number(row.assigned_vehicle)===Number(driver.vehicle_number)),completion=completionOf(driver.id);return deliveries.length>0||urgent||Number(completion?.deliveryCount)>0}
    function workDrivers(){return drivers().filter(driver=>driver.active&&hasWorkForDate(driver))}
    function shipmentRows(){const rows=[];data.snapshots.forEach(snapshot=>{const profile=data.profiles.find(p=>p.id===snapshot.driver_id),payload=snapshot.payload||{},completion=payload.workClosures?.[workDate]||null,vehicle=Number(payload.profile?.vehicleNumber||profile?.vehicle_number)||0,driver=payload.profile?.driverName||profile?.full_name||"배송원 미확인",customers=payload.customers||[];(payload.deliveries||[]).filter(delivery=>dateKey(delivery.deliveryDate)===dateKey(workDate)&&delivery.status!=="cancelled").forEach(delivery=>{const store=customers.find(c=>c.id===delivery.customerId)?.name||delivery.fallbackStoreName||"거래처 미확인";(delivery.items||[]).forEach(item=>{const weights=Array.isArray(item.weighings)?item.weighings:[],measuredKg=weights.reduce((sum,w)=>sum+(Number(w.weight)||0),0),manualKg=Number(item.actualWeight)||0,qty=Number(item.requestedQty)||0,unit=String(item.unit||"개"),kg=measuredKg>0?measuredKg:manualKg>0?manualKg:unit==="kg"?qty:0,pieces=weights.length>0?weights.length:unit==="마리"?qty:0,weighable=item.category==="활어";rows.push({driverId:snapshot.driver_id,vehicle,driver,deliveryId:delivery.id,routeOrder:Number(delivery.routeOrder)||0,store,species:String(item.species||item.item||"품목명 미입력").trim(),category:String(item.category||"기타"),origin:String(item.originCountry||item.origin||""),unit,requestedQty:qty,pieces,kg:Number(kg.toFixed(3)),kgSource:measuredKg>0?"개별계근":manualKg>0?"실중량":unit==="kg"?"주문 kg":"없음",deliveryStatus:completion?"completed":"working",wholeCompletionAt:completion?.at||"",completionResult:completion?.result||"",weighable,weighingConfirmed:weighable?Boolean(item.weighingConfirmed):true,snapshotUpdatedAt:snapshot.updated_at||""})})})});return rows}
    function aggregateShipments(rows,byVehicle=false){const groups=new Map();rows.forEach(row=>{const productKey=`${row.category}::${row.species}`,key=byVehicle?`${row.vehicle}::${productKey}`:productKey,current=groups.get(key)||{vehicle:byVehicle?row.vehicle:null,driver:byVehicle?row.driver:"",species:row.species,category:row.category,pieces:0,kg:0,lines:0,unconfirmedLines:0,unitTotals:{},stores:new Set(),vehicles:new Set()};current.pieces+=Number(row.pieces)||0;current.kg+=Number(row.kg)||0;current.lines+=1;if(row.weighable&&!row.weighingConfirmed)current.unconfirmedLines+=1;current.unitTotals[row.unit]=(current.unitTotals[row.unit]||0)+(Number(row.requestedQty)||0);current.stores.add(row.store);current.vehicles.add(row.vehicle);groups.set(key,current)});return [...groups.values()].map(group=>({...group,kg:Number(group.kg.toFixed(3)),storeCount:group.stores.size,vehicleCount:group.vehicles.size})).sort((a,b)=>byVehicle?(a.vehicle-b.vehicle||a.category.localeCompare(b.category,"ko")||a.species.localeCompare(b.species,"ko")):(b.kg-a.kg||b.pieces-a.pieces||a.category.localeCompare(b.category,"ko")||a.species.localeCompare(b.species,"ko")))}
    function qtyText(value){return Number(value||0).toLocaleString("ko-KR",{maximumFractionDigits:3})}
    function shipmentUnitText(unitTotals,excludePieces=false){const entries=Object.entries(unitTotals||{}).filter(([unit,value])=>Number(value)>0&&(!excludePieces||unit!=="마리"));return entries.length?entries.map(([unit,value])=>`${qtyText(value)}${unit}`).join(" · "):"-"}
    function kgText(value){return `${Number(value||0).toLocaleString("ko-KR",{minimumFractionDigits:3,maximumFractionDigits:3})}kg`}
    function filteredShipmentRows(){const q=search.trim().toLowerCase();return shipmentRows().filter(row=>(shipmentVehicle==="all"||String(row.vehicle)===shipmentVehicle)&&(shipmentStatus==="all"||(shipmentStatus==="completed"?row.deliveryStatus==="completed":row.deliveryStatus!=="completed"))&&(!q||`${row.species} ${row.category} ${row.store} ${row.driver} ${row.vehicle}`.toLowerCase().includes(q)))}
    function renderShipments(){const all=shipmentRows(),rows=filteredShipmentRows(),products=aggregateShipments(rows),vehicles=aggregateShipments(rows,true),vehicleOptions=[...new Set(all.map(row=>row.vehicle).filter(Boolean))].sort((a,b)=>a-b),totalPieces=rows.reduce((sum,row)=>sum+Number(row.pieces||0),0),totalKg=rows.reduce((sum,row)=>sum+Number(row.kg||0),0),unconfirmed=rows.filter(row=>row.weighable&&!row.weighingConfirmed).length,lastSync=data.snapshots.map(s=>new Date(s.updated_at||0).getTime()).reduce((a,b)=>Math.max(a,b),0);const productCards=products.map(product=>{const primary=[product.pieces>0?`${qtyText(product.pieces)}마리`:"",product.kg>0?kgText(product.kg):""].filter(Boolean).join(" · ")||shipmentUnitText(product.unitTotals);return `<article class="info-card"><div class="board-top"><h3>${esc(product.species)}</h3><span class="badge">${esc(product.category)}</span></div><div class="product-price">${esc(primary)}</div><p><b>발주·포장 단위:</b> ${esc(shipmentUnitText(product.unitTotals))}</p><div class="tags"><span class="badge green">${product.vehicleCount}개 호차</span><span class="badge purple">${product.storeCount}개 납품처</span>${product.unconfirmedLines?`<span class="badge red">계근 미확정 ${product.unconfirmedLines}건</span>`:`<span class="badge green">계근 확인</span>`}</div></article>`}).join("");const productTable=products.map(product=>`<tr><td><strong>${esc(product.species)}</strong><br><small>${esc(product.category)}</small></td><td class="money">${product.pieces>0?qtyText(product.pieces)+"마리":"-"}</td><td class="money">${product.kg>0?kgText(product.kg):"-"}</td><td>${esc(shipmentUnitText(product.unitTotals))}</td><td>${product.vehicleCount}개</td><td>${product.storeCount}곳</td><td><span class="badge ${product.unconfirmedLines?"red":"green"}">${product.unconfirmedLines?product.unconfirmedLines+"건 미확정":"확인"}</span></td></tr>`).join("");const vehicleTable=vehicles.map(row=>`<tr><td><span class="vehicle">${row.vehicle||"-"}호</span><br><small>${esc(row.driver)}</small></td><td><strong>${esc(row.species)}</strong><br><small>${esc(row.category)}</small></td><td class="money">${row.pieces>0?qtyText(row.pieces)+"마리":"-"}</td><td class="money">${row.kg>0?kgText(row.kg):"-"}</td><td>${esc(shipmentUnitText(row.unitTotals))}</td><td>${row.storeCount}곳</td><td><span class="badge ${row.unconfirmedLines?"red":"green"}">${row.unconfirmedLines?row.unconfirmedLines+"건 미확정":"확인"}</span></td></tr>`).join("");const tools=`<div class="filters" style="flex-wrap:wrap"><input id="searchInput" class="control search" value="${esc(search)}" placeholder="제품·거래처·배송원 검색"><select id="shipmentVehicleFilter" class="control"><option value="all">전체 호차</option>${vehicleOptions.map(vehicle=>`<option value="${vehicle}" ${shipmentVehicle===String(vehicle)?"selected":""}>${vehicle}호차</option>`).join("")}</select><select id="shipmentStatusFilter" class="control"><option value="all" ${shipmentStatus==="all"?"selected":""}>출고 대상 전체</option><option value="completed" ${shipmentStatus==="completed"?"selected":""}>납품 완료만</option><option value="working" ${shipmentStatus==="working"?"selected":""}>준비·배송 중</option></select></div>`;$("#content").innerHTML=pageHead("당일 제품별 출고량",`${workDate} 기준 · 취소 배송을 제외하고 제품별 마릿수·kg·팩·박스 수량을 호차별로 자동 합산합니다.`,`<button class="btn purple" data-action="export-shipments">출고량 Excel</button>`)+metrics([["출고 제품",products.length+"종"],["총 마릿수",qtyText(totalPieces)+"마리"],["총 kg",kgText(totalKg)],["출고 호차",new Set(rows.map(row=>row.vehicle).filter(Boolean)).size+"대"],["계근 미확정",unconfirmed+"건",unconfirmed?"bad":""],["최근 동기화",lastSync?new Date(lastSync).toLocaleTimeString("ko-KR",{hour:"2-digit",minute:"2-digit"}):"자료 없음"]])+panel("제품별 전체 합계","광어·도미 등 같은 제품을 모든 호차에서 합산합니다. 활어와 필렛은 서로 분리됩니다.",`<div class="card-grid">${productCards||`<div class="empty">선택한 날짜에 동기화된 출고 품목이 없습니다.</div>`}</div>`,tools)+panel("제품별 상세 합계","마리·kg와 팩·박스·망 등 원래 발주 단위를 함께 표시합니다.",`<div class="table-scroll" style="display:block"><table class="table"><thead><tr><th>제품</th><th>총 마릿수</th><th>총 kg</th><th>단위별 수량</th><th>호차</th><th>납품처</th><th>계근</th></tr></thead><tbody>${productTable||`<tr><td colspan="7" class="empty">집계 자료가 없습니다.</td></tr>`}</tbody></table></div>`)+panel("호차별·제품별 출고량","각 호차가 제품별로 몇 마리, 몇 kg을 출고하는지 확인합니다.",`<div class="table-scroll" style="display:block"><table class="table"><thead><tr><th>호차·배송원</th><th>제품</th><th>마릿수</th><th>kg</th><th>단위별 수량</th><th>납품처</th><th>계근</th></tr></thead><tbody>${vehicleTable||`<tr><td colspan="7" class="empty">호차별 자료가 없습니다.</td></tr>`}</tbody></table></div>`)}
    function exportShipmentExcel(){if(!window.XLSX)return toast("Excel 기능을 불러오지 못했습니다.",true);const rows=filteredShipmentRows(),products=aggregateShipments(rows),vehicles=aggregateShipments(rows,true),wb=XLSX.utils.book_new(),add=(name,list)=>XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(list.length?list:[{내용:"자료 없음"}]),name);add("제품별 전체합계",products.map(row=>({날짜:workDate,제품:row.species,구분:row.category,"총 마릿수":row.pieces,"총 kg":row.kg,"단위별 수량":shipmentUnitText(row.unitTotals),출고호차수:row.vehicleCount,납품처수:row.storeCount,계근미확정건수:row.unconfirmedLines})));add("호차별 제품합계",vehicles.map(row=>({날짜:workDate,호차:row.vehicle,배송원:row.driver,제품:row.species,구분:row.category,마릿수:row.pieces,"총 kg":row.kg,"단위별 수량":shipmentUnitText(row.unitTotals),납품처수:row.storeCount,계근미확정건수:row.unconfirmedLines})));add("출고 상세",rows.map(row=>({날짜:workDate,호차:row.vehicle,배송원:row.driver,배송순서:row.routeOrder,거래처:row.store,제품:row.species,구분:row.category,원산지:row.origin,발주수량:row.requestedQty,단위:row.unit,마릿수:row.pieces,"집계 kg":row.kg,"kg 기준":row.kgSource,계근확정:row.weighable?(row.weighingConfirmed?"확정":"미확정"):"해당 없음",배송상태:row.deliveryStatus})));XLSX.writeFile(wb,`장백수산_제품별_출고량_${workDate}.xlsx`);toast("제품별·호차별 출고량 Excel을 저장했습니다.")}
    function purchaseRecords(){
      return drivers().filter(driver=>driver.active).map(driver=>{
        const snapshot=data.snapshots.find(row=>row.driver_id===driver.id),payload=snapshot?.payload||{},submission=payload.purchaseSubmissions?.[workDate]||null,currentFingerprint=payload.purchaseCurrentFingerprints?.[workDate]||"",deliveries=(payload.deliveries||[]).filter(delivery=>dateKey(delivery.deliveryDate)===dateKey(workDate)&&delivery.status!=="cancelled"),hasOrders=deliveries.some(delivery=>(delivery.items||[]).length>0),status=submission?(currentFingerprint&&submission.fingerprint!==currentFingerprint?"changed":"submitted"):(hasOrders?"pending":"empty");
        return {driverId:driver.id,vehicle:Number(payload.profile?.vehicleNumber||driver.vehicle_number)||0,driver:payload.profile?.driverName||driver.full_name||"배송원 미확인",status,submission,groups:Array.isArray(submission?.groups)?submission.groups:[],hasOrders,lastSync:snapshot?.updated_at||""};
      }).sort((a,b)=>a.vehicle-b.vehicle)
    }
    function aggregatePurchases(records){
      const groups=new Map();
      records.filter(record=>record.status==="submitted").forEach(record=>record.groups.forEach(item=>{
        const key=[item.category,item.species,item.size,item.originCountry,item.processing,item.unit,item.includedNote,item.rubberBand?`${item.rubberBandColor}-${item.rubberBandQuantity}`:""].join("||"),current=groups.get(key)||{category:item.category||"기타",species:item.species||"품목명 미입력",size:item.size||"",originCountry:item.originCountry||"",processing:item.processing||"",unit:item.unit||"개",includedNote:item.includedNote||"",rubberBand:Boolean(item.rubberBand),rubberBandColor:item.rubberBandColor||"",rubberBandQuantity:Number(item.rubberBandQuantity)||0,total:0,vehicles:new Set(),stores:new Set()};
        current.total+=Number(item.total)||0;current.vehicles.add(record.vehicle);(item.stores||[]).forEach(store=>current.stores.add(store.name));groups.set(key,current)
      }));
      return [...groups.values()].map(group=>({...group,total:Number(group.total.toFixed(3)),vehicleCount:group.vehicles.size,storeCount:group.stores.size})).sort((a,b)=>a.category.localeCompare(b.category,"ko")||a.species.localeCompare(b.species,"ko")||a.size.localeCompare(b.size,"ko"))
    }
    function purchaseStatusBadge(status){return status==="submitted"?`<span class="badge green">제출 완료</span>`:status==="changed"?`<span class="badge red">변경·재제출 필요</span>`:status==="pending"?`<span class="badge amber">작성·제출 대기</span>`:`<span class="badge">발주 없음</span>`}
    function renderPurchases(){
      const records=purchaseRecords(),submitted=records.filter(row=>row.status==="submitted"),changed=records.filter(row=>row.status==="changed"),pending=records.filter(row=>row.status==="pending"),totals=aggregatePurchases(records),lastSubmit=submitted.map(row=>new Date(row.submission?.submittedAt||0).getTime()).reduce((a,b)=>Math.max(a,b),0);
      const cards=records.map(row=>`<article class="info-card"><div class="board-top"><h3>${row.vehicle||"-"}호 · ${esc(row.driver)}</h3>${purchaseStatusBadge(row.status)}</div><p>${row.submission?`${row.submission.revision||1}차 제출 · ${fmt(row.submission.submittedAt)}`:row.hasOrders?"발주 입력은 있으나 매입전표를 제출하지 않았습니다.":"해당 날짜 발주가 없습니다."}</p>${row.submission?.note?`<p><b>전달 메모:</b> ${esc(row.submission.note)}</p>`:""}<div class="tags"><span class="badge purple">품목 ${row.groups.length}종</span><span class="badge">최근 동기화 ${row.lastSync?new Date(row.lastSync).toLocaleTimeString("ko-KR",{hour:"2-digit",minute:"2-digit"}):"없음"}</span></div>${row.groups.length?`<div class="row-actions"><button class="mini" data-action="purchase-details" data-id="${row.driverId}">호차 전표 상세</button></div>`:""}</article>`).join("");
      const productRows=totals.map(item=>`<tr><td><strong>${esc(item.species)}</strong><br><small>${esc(item.category)}</small></td><td>${esc(item.size||"-")}<br><small>${esc(item.originCountry||"-")} · ${esc(item.processing||"-")}</small></td><td class="money">${qtyText(item.total)}${esc(item.unit)}</td><td>${item.vehicleCount}개</td><td>${item.storeCount}곳</td><td>${item.rubberBand?`${esc(item.rubberBandColor)} ${item.rubberBandQuantity}개`:"-"}</td><td>${esc(item.includedNote||"-")}</td></tr>`).join("");
      $("#content").innerHTML=pageHead("호차별 매입전표",`${workDate} 기준 · 배송원이 제출한 매입전표를 구매 담당자가 제품별로 통합 확인합니다.`,`<button class="btn purple" data-action="export-purchases">매입전표 Excel</button>`)+metrics([["대상 호차",records.length+"대"],["제출 완료",submitted.length+"대"],["재제출 필요",changed.length+"대",changed.length?"bad":""],["제출 대기",pending.length+"대",pending.length?"warn":""],["통합 품목",totals.length+"종"],["최근 제출",lastSubmit?new Date(lastSubmit).toLocaleTimeString("ko-KR",{hour:"2-digit",minute:"2-digit"}):"없음"]])+panel("호차별 제출 현황","발주가 변경된 호차는 반드시 다시 제출해야 통합 합계에 반영됩니다.",`<div class="card-grid">${cards||`<div class="empty">활성 배송원이 없습니다.</div>`}</div>`)+panel("구매 담당자 통합 매입 수량","현재 제출 완료 상태인 호차의 매입전표만 합산합니다.",`<div class="table-scroll" style="display:block"><table class="table"><thead><tr><th>제품</th><th>규격·원산지·작업</th><th>총 매입 수량</th><th>호차</th><th>거래처</th><th>밴드</th><th>메모</th></tr></thead><tbody>${productRows||`<tr><td colspan="7" class="empty">제출 완료된 매입전표가 없습니다.</td></tr>`}</tbody></table></div>`)
    }
    function showPurchaseDetails(driverId){
      const row=purchaseRecords().find(record=>record.driverId===driverId);if(!row)return;
      const items=row.groups.map(item=>`<article class="info-card"><div class="board-top"><h3>${esc(item.species)} · ${esc(item.size||"-")}</h3><strong>${qtyText(item.total)}${esc(item.unit||"개")}</strong></div><p>${esc(item.category||"기타")} · ${esc(item.originCountry||"-")} · ${esc(item.processing||"-")}</p>${(item.stores||[]).map(store=>`<p><b>${esc(store.name)}</b> · ${qtyText(store.qty)}${esc(item.unit||"개")}</p>`).join("")}${item.rubberBand?`<p>밴드 ${esc(item.rubberBandColor)} ${item.rubberBandQuantity}개</p>`:""}${item.includedNote?`<p>${esc(item.includedNote)}</p>`:""}</article>`).join("");
      modal(`${row.vehicle}호 · ${esc(row.driver)} 매입전표`,`${row.submission?.revision||1}차 제출 · ${fmt(row.submission?.submittedAt)}`,`<div class="modal-body"><div class="${row.status==="submitted"?"notice":"message"}">${row.status==="submitted"?"구매 담당자 제출 완료":"발주 변경으로 재제출이 필요합니다."}</div><div class="card-grid" style="grid-template-columns:1fr">${items||`<div class="empty">제출 품목이 없습니다.</div>`}</div></div>`,true)
    }
    function exportPurchaseExcel(){
      if(!window.XLSX)return toast("Excel 기능을 불러오지 못했습니다.",true);const records=purchaseRecords(),submitted=records.filter(row=>row.status==="submitted"),totals=aggregatePurchases(records),wb=XLSX.utils.book_new(),add=(name,rows)=>XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(rows.length?rows:[{내용:"자료 없음"}]),name);
      add("통합 매입전표",totals.map(item=>({날짜:workDate,제품:item.species,구분:item.category,규격:item.size,원산지:item.originCountry,작업:item.processing,"총 매입수량":item.total,단위:item.unit,호차수:item.vehicleCount,거래처수:item.storeCount,밴드:item.rubberBand?`${item.rubberBandColor} ${item.rubberBandQuantity}개`:"",메모:item.includedNote})));
      add("호차별 매입전표",submitted.flatMap(row=>row.groups.map(item=>({날짜:workDate,호차:row.vehicle,배송원:row.driver,제출차수:row.submission.revision,제출시간:row.submission.submittedAt,제품:item.species,구분:item.category,규격:item.size,원산지:item.originCountry,작업:item.processing,매입수량:item.total,단위:item.unit,거래처별:(item.stores||[]).map(store=>`${store.name} ${store.qty}${item.unit}`).join(" · "),전달메모:row.submission.note||""}))));
      add("제출 현황",records.map(row=>({날짜:workDate,호차:row.vehicle,배송원:row.driver,상태:({submitted:"제출 완료",changed:"변경·재제출 필요",pending:"작성·제출 대기",empty:"발주 없음"})[row.status],제출차수:row.submission?.revision||"",제출시간:row.submission?.submittedAt||"",품목수:row.groups.length,전달메모:row.submission?.note||"",최근동기화:row.lastSync||""})));
      XLSX.writeFile(wb,`장백수산_통합_매입전표_${workDate}.xlsx`);toast("통합 매입전표 Excel을 저장했습니다.")
    }
    function renderDashboard(){const allActive=drivers().filter(x=>x.active),active=workDrivers(),activeIds=new Set(active.map(row=>row.id)),urgent=data.urgent.filter(x=>["new","acknowledged","loaded"].includes(x.status)),inc=data.incidents.filter(x=>!["resolved","closed"].includes(x.status)),approved=data.departures.filter(x=>x.approved&&activeIds.has(x.driver_id)).length,closed=data.workday?.is_closed,hasDailyWork=active.length>0||urgent.length>0||inc.length>0;
      const close=hasDailyWork?`<div class="close-banner ${closed?"closed":""}"><div><strong>${closed?"✓ 당일 업무가 마감·잠금되었습니다.":"당일 업무가 진행 중입니다."}</strong><div>${closed?`${fmt(data.workday.closed_at)} · ${esc(data.workday.close_reason||"마감")}`:"마감하면 배송원 자료 수정이 잠기며 회계 자료가 보존됩니다."}</div></div>${can("departure_write")||can("accounting_write")?`<button class="btn ${closed?"warn":"primary"}" data-action="toggle-close" type="button">${closed?"마감 해제":"오늘 업무 마감"}</button>`:""}</div>`:`<div class="close-banner"><div><strong>${workDate}에 등록된 배송 업무가 없습니다.</strong><div>배송원이 해당 날짜의 발주를 입력하거나 긴급 발주가 등록되면 자동으로 표시됩니다.</div></div></div>`;
      $("#content").innerHTML=pageHead("오늘 통합 관제","선택한 날짜에 실제 업무가 있는 배송원의 출차 승인과 배송 진행 상황을 확인합니다.")+close+metrics([["등록 배송원",allActive.length],["당일 업무 배송원",active.length],["출차 승인",`${approved}/${active.length}`],["긴급 미완료",urgent.length,urgent.length?"warn":""],["처리할 오류",inc.length,inc.length?"bad":""],["전체 배송 완료",active.filter(x=>Boolean(completionOf(x.id))).length]])+renderDepartureBoard()+renderCompletionBoard()+renderHealth();}
    function renderDepartureBoard(){const ds=workDrivers();const cards=ds.map(d=>{const s=statusOf(d.id),c=data.departures.find(x=>x.driver_id===d.id)||{},ready=s.work_state==="departure_ready"||c.driver_ready,checks=[["발주",c.orders_checked],["수조",c.tanks_checked||s.tank_total&&s.tank_loaded===s.tank_total],["박스",c.boxes_checked||s.box_total===s.box_checked],["긴급",c.urgent_checked||!s.urgent_pending],["인계",c.handover_checked]];return `<article class="board-card ${ready?"ready":""} ${c.approved?"approved":""}"><div class="board-top"><h3>${d.vehicle_number||"-"}호 · ${esc(d.full_name)}</h3>${c.approved?`<span class="badge green">승인</span>`:`<span class="badge ${ready?"amber":"red"}">${ready?"승인대기":"점검중"}</span>`}</div><small>${stateLabel(s.work_state)} · 당일 배정 ${datedDeliveries(d.id,workDate,false).length||s.delivery_total}곳</small><div class="checks">${checks.map(x=>`<span class="check ${x[1]?"ok":""}">${x[1]?"✓":"○"} ${x[0]}</span>`).join("")}</div><div class="board-actions"><button class="mini" data-action="driver-details" data-id="${d.id}">상세</button>${can("departure_write")?`<button class="mini" data-action="departure-approve" data-id="${d.id}" ${c.approved?"disabled":""}>${c.approved?"승인완료":"출차 승인"}</button>`:""}</div></article>`}).join("");return panel("1순위 · 출차 승인 보드","선택 날짜의 발주·상차·박스·긴급 발주를 확인하고 승인합니다.",`<div class="board">${cards||`<div class="empty">${workDate}에 등록된 배송 업무가 없습니다.</div>`}</div>`) }
    function renderCompletionBoard(){const ds=workDrivers();const cards=ds.map(d=>{const s=statusOf(d.id),c=completionOf(d.id),problem=c?.result==="issue",last=c?.lastSyncAt||s.last_sync_at;return `<article class="board-card ${c&&!problem?"approved":""} ${problem?"urgent-card":""}"><div class="board-top"><h3>${d.vehicle_number||"-"}호 · ${esc(d.full_name)}</h3>${c?`<span class="badge ${problem?"red":"green"}">${problem?"관리자 확인 필요":"전체 완료"}</span>`:`<span class="badge amber">${stateLabel(s.work_state)}</span>`}</div><p><b>전체 완료 시간:</b> ${c?fmt(c.at):"-"}</p><p><b>당일 배정:</b> ${c?.deliveryCount??datedDeliveries(d.id,workDate,false).length}곳 · <b>긴급 미처리:</b> ${c?.urgentPending??s.urgent_pending}건 · <b>미해결 오류:</b> ${c?.issueCount??s.issue_count}건</p>${c?.note?`<p><b>완료 메모:</b> ${esc(c.note)}</p>`:""}<small>마지막 동기화 ${fmt(last)}</small><div class="board-actions"><button class="mini" data-action="driver-details" data-id="${d.id}">전체 상황 상세</button></div></article>`}).join("");return panel("호차별 전체 배송 완료 현황","선택 날짜의 배송원별 전체 완료 시간, 오류 및 완료 메모를 확인합니다.",`<div class="board">${cards||`<div class="empty">${workDate}에 등록된 배송 업무가 없습니다.</div>`}</div>`)}
    function renderHealth(){const activeRows=workDrivers(),active=activeRows.length,snaps=activeRows.filter(row=>snapshotForDriver(row.id)).length,stale=activeRows.filter(row=>!online(statusOf(row.id))).length,healthy=active===0||snaps>=Math.max(0,active-stale);return panel("서버·자동 백업 상태","선택 날짜의 업무 자료만 기준으로 확인합니다.",`<div class="health"><article class="health-card"><small>당일 중앙 백업</small><strong>${snaps}/${active}호차</strong></article><article class="health-card"><small>2분 이상 미접속</small><strong>${stale}명</strong></article><article class="health-card"><small>종합 상태</small><strong style="color:${healthy?"var(--green)":"var(--red)"}">${healthy?"정상":"확인 필요"}</strong></article></div>`) }
    function renderDrivers(){const list=drivers().filter(d=>!search||`${d.full_name} ${d.employee_code} ${d.vehicle_number}`.toLowerCase().includes(search.toLowerCase())),highest=can("role_manage"),editable=can("driver_write");const actionButtons=d=>`<button class="mini" data-action="driver-history" data-id="${d.id}">배정 이력</button>${editable?`<button class="mini" data-action="driver-edit" data-id="${d.id}">전체 정보 수정</button>`:""}${highest?`<button class="mini" data-action="driver-delete" data-id="${d.id}" style="color:var(--red);border-color:#efb9bd">삭제</button>`:""}`;const rows=list.map(d=>{const s=statusOf(d.id),c=completionOf(d.id);return `<tr><td><div class="name"><span class="avatar">${esc(d.full_name.slice(-2))}</span><span><strong>${esc(d.full_name)}</strong><small>${esc(d.employee_code)} · ${esc(d.phone||"연락처 없음")}</small></span></div></td><td><span class="vehicle">${d.vehicle_number||"-"}호</span></td><td><span class="badge ${d.active?(online(s)?"green":""):"red"}">${d.active?(online(s)?"접속 중":"오프라인"):"사용정지"}</span></td><td>${c?`<span class="badge ${c.result==="issue"?"red":"green"}">${c.result==="issue"?"전체 완료·확인 필요":"전체 배송 완료"}</span><br><small>${fmt(c.at)}</small>`:`${stateLabel(s.work_state)}<br><small>당일 배정 ${s.delivery_total}곳</small>`}</td><td><div class="row-actions">${actionButtons(d)}</div></td></tr>`}).join("");const cards=list.map(d=>`<article class="mobile-card"><div class="name"><span class="avatar">${esc(d.full_name.slice(-2))}</span><span><strong>${esc(d.full_name)} · ${d.vehicle_number||"-"}호</strong><small>${esc(d.employee_code)} · ${d.active?"사용 중":"정지"}</small></span></div><div class="row-actions">${actionButtons(d)}</div></article>`).join("");$("#content").innerHTML=pageHead("배송원·호차 관리","최고 관리자는 로그인 아이디를 포함한 배송원 정보를 수정하고 계정을 삭제할 수 있습니다.",editable?`<button class="btn primary" data-action="driver-new">＋ 배송원</button>`:"")+panel("배송원 계정과 현재 배정","아이디 변경은 로그인 계정에도 즉시 반영되고, 호차 변경 시 이전 배정은 자동 종료됩니다.",`<div class="table-scroll"><table class="table"><thead><tr><th>배송원</th><th>호차</th><th>접속</th><th>업무</th><th>관리</th></tr></thead><tbody>${rows}</tbody></table></div><div class="mobile-cards">${cards}</div>`,`<div class="filters"><input id="searchInput" class="control search" value="${esc(search)}" placeholder="이름·아이디·호차 검색"></div>`);}
    function renderStores(){const list=data.stores.filter(s=>!search||`${s.name} ${s.address} ${s.assigned_vehicle}`.toLowerCase().includes(search.toLowerCase()));const cards=list.map(s=>`<article class="info-card"><div class="board-top"><h3>${s.delivery_order||"-"}번 · ${esc(s.name)}</h3><span class="vehicle">${s.assigned_vehicle||"-"}호</span></div><p>${esc(s.address||"주소 미입력")} · ${esc(s.phone||"연락처 미입력")}</p><div class="tags"><span class="badge purple">입고 ${esc(s.preferred_time||"미입력")}</span><span class="badge ${s.active?"green":"red"}">${s.active?"거래 중":"중지"}</span></div><p><b>진입:</b> ${esc(s.entry_method||"미입력")}</p><p><b>놓는 곳:</b> ${esc(s.placement||"미입력")}</p><p><b>금지사항:</b> ${esc(s.prohibited||"없음")}</p><div class="row-actions"><button class="mini" data-action="store-print" data-id="${s.id}">인수인계표</button>${can("store_secret")?`<button class="mini" data-action="store-secret" data-id="${s.id}">비밀번호</button>`:""}${can("store_write")?`<button class="mini" data-action="store-edit" data-id="${s.id}">수정</button>`:""}</div></article>`).join("");$("#content").innerHTML=pageHead("거래처·인수인계","새 배송원이 바로 이해하도록 진입·놓는 자리·금지사항·입고 시간을 표준화합니다.",can("store_write")?`<button class="btn primary" data-action="store-new">＋ 거래처</button>`:"")+panel("중앙 거래처 목록","호차와 배송 순서대로 정렬됩니다. 출입 비밀번호 열람은 별도 기록됩니다.",`<div class="card-grid">${cards||`<div class="empty">등록된 거래처가 없습니다.</div>`}</div>`,`<div class="filters"><input id="searchInput" class="control search" value="${esc(search)}" placeholder="거래처·주소·호차 검색"></div>`);}
    function renderUrgent(){const status={new:"신규",acknowledged:"배송원 확인",loaded:"상차 완료",delivered:"납품 완료",impossible:"처리 불가",cancelled:"취소"},incident={reported:"접수",checking:"확인 중",resolved:"조치 완료",closed:"종결"};const u=data.urgent.map(x=>`<article class="info-card urgent-card"><div class="board-top"><h3>${x.assigned_vehicle||"-"}호 · ${esc(x.store_name||"거래처 미지정")}</h3><span class="badge ${["delivered","cancelled"].includes(x.status)?"green":"red"}">${status[x.status]}</span></div><p><b>${esc(x.item_summary)}</b> ${esc(x.quantity_summary)}</p><p>${esc(x.note||"메모 없음")}</p><small>등록 ${fmt(x.created_at)}${x.acknowledged_at?` · 확인 ${fmt(x.acknowledged_at)}`:""}</small><div class="row-actions">${can("urgent_write")?`<button class="mini" data-action="urgent-status" data-id="${x.id}">상태 변경</button><button class="mini" data-action="urgent-edit" data-id="${x.id}">수정</button>`:""}</div></article>`).join("");const inc=data.incidents.map(x=>`<article class="info-card incident-card"><div class="board-top"><h3>${x.vehicle_number||"-"}호 · ${esc(({wrong_delivery:"오배송",shortage:"수량 부족",excess:"수량 초과",damage:"파손",band_mismatch:"밴드 오류",tank_mismatch:"수조 오류",delay:"지연",other:"기타"})[x.incident_type]||x.incident_type)}</h3><span class="badge ${["resolved","closed"].includes(x.status)?"green":"amber"}">${incident[x.status]}</span></div><p>${esc(x.description)}</p><p><b>조치:</b> ${esc(x.resolution||"미입력")}</p><div class="row-actions">${can("incident_write")?`<button class="mini" data-action="incident-manage" data-id="${x.id}">조치·상태</button>`:""}</div></article>`).join("");$("#content").innerHTML=pageHead("긴급 발주·오류 관리","등록→배송원 확인→상차→납품 상태와 사고 조치 이력을 남깁니다.",can("urgent_write")?`<button class="btn primary" data-action="urgent-new">＋ 긴급 발주</button>`:"")+metrics([["긴급 전체",data.urgent.length],["미확인",data.urgent.filter(x=>x.status==="new").length,"bad"],["배송원 확인",data.urgent.filter(x=>x.status==="acknowledged").length,"warn"],["상차 완료",data.urgent.filter(x=>x.status==="loaded").length],["오류 접수",data.incidents.filter(x=>x.status==="reported").length,"bad"],["조치 완료",data.incidents.filter(x=>["resolved","closed"].includes(x.status)).length]])+`<div class="grid-2">${panel("긴급 추가 발주","배송원 화면에 자동 표시되며 상태가 실시간 공유됩니다.",`<div class="card-grid" style="grid-template-columns:1fr">${u||`<div class="empty">긴급 발주가 없습니다.</div>`}</div>`)}${panel("오배송·오류 보고","오류 원인과 조치 결과를 보존합니다.",`<div class="card-grid" style="grid-template-columns:1fr">${inc||`<div class="empty">접수된 오류가 없습니다.</div>`}</div>`)}</div>`;}
    function renderProducts(){const cards=data.products.filter(p=>!search||`${p.name} ${p.product_code} ${p.origin_country}`.toLowerCase().includes(search.toLowerCase())).map(p=>{const overrides=data.prices.filter(x=>x.product_id===p.id&&x.store_id).length;return `<article class="info-card"><div class="board-top"><h3>${esc(p.name)}</h3><span class="badge">${esc(p.category)}</span></div><p>${esc(p.product_code)} · ${esc(p.origin_country)} · ${esc(p.unit)} 단위</p><div class="product-price">${money(p.base_price)}원 / ${esc(p.unit)}</div>${p.category==="필렛 제품"?`<p><b>필렛 작업비:</b> kg당 ${money(p.fillet_fee_per_kg)}원</p>`:""}<p>기본 밴드: ${esc(p.default_band_color||"없음")} · 거래처별 단가 ${overrides}건</p><div class="row-actions">${can("product_write")?`<button class="mini" data-action="price-new" data-id="${p.id}">거래처 단가</button><button class="mini" data-action="product-edit" data-id="${p.id}">수정</button>`:""}</div></article>`}).join("");$("#content").innerHTML=pageHead("품목·가격 중앙관리","kg·마리·팩 단위, 원산지, 필렛 작업비, 거래처별 적용기간을 관리합니다.",can("product_write")?`<button class="btn primary" data-action="product-new">＋ 품목</button>`:"")+metrics([["전체 품목",data.products.length],["활어",data.products.filter(x=>x.category==="활어").length],["패류",data.products.filter(x=>x.category==="패류").length],["냉동·팩",data.products.filter(x=>x.category==="냉동제품").length],["필렛",data.products.filter(x=>x.category==="필렛 제품").length],["거래처 단가",data.prices.filter(x=>x.store_id).length]])+panel("중앙 품목표","수정 후 새 발주에 동일한 기준을 적용할 수 있습니다.",`<div class="card-grid">${cards||`<div class="empty">등록된 품목이 없습니다.</div>`}</div>`,`<div class="filters"><input id="searchInput" class="control search" value="${esc(search)}" placeholder="품목·코드·원산지 검색"></div>`);}
    function latestInvoices(){const map=new Map();data.invoices.forEach(invoice=>{const current=map.get(invoice.invoice_no);if(!current||Number(invoice.revision_no)>Number(current.revision_no))map.set(invoice.invoice_no,invoice)});return [...map.values()]}
    function accountingItemAmounts(item){const weighings=Array.isArray(item.weighings)?item.weighings:[],weighed=weighings.reduce((sum,row)=>sum+(Number(row.weight)||0),0),actual=weighed>0?weighed:Number(item.actualWeight)||0,requested=Number(item.requestedQty)||0,priceQty=item.priceUnit==="kg"?(actual||requested):requested,subtotal=priceQty*(Number(item.unitPrice)||0),filletFee=item.category==="필렛 제품"?(actual||requested)*(Number(item.processingFeePerKg)||0):0;return{subtotal,filletFee,actual}}
    function operationalAccountingRows(){const latest=latestInvoices(),linked=new Map(),used=new Set();latest.forEach(invoice=>{const sourceId=invoice.detail?.sourceDeliveryId,sourceDriver=invoice.detail?.sourceDriverId||invoice.driver_id;if(sourceId!=null&&sourceDriver)linked.set(`${sourceDriver}:${sourceId}`,invoice)});const rows=[];data.snapshots.forEach(snapshot=>{const profile=data.profiles.find(row=>row.id===snapshot.driver_id),payload=snapshot.payload||{},customers=Array.isArray(payload.customers)?payload.customers:[],vehicle=Number(payload.profile?.vehicleNumber||profile?.vehicle_number)||0,driver=payload.profile?.driverName||profile?.full_name||"배송원 미확인";datedDeliveries(snapshot.driver_id,workDate,false).forEach(delivery=>{const sourceKey=`${snapshot.driver_id}:${delivery.id}`,invoice=linked.get(sourceKey)||null;if(invoice)used.add(invoice.id);const storeName=customers.find(customer=>String(customer.id)===String(delivery.customerId))?.name||delivery.fallbackStoreName||"거래처 미확인",store=data.stores.find(row=>row.name===storeName)||null,amounts=(delivery.items||[]).map(accountingItemAmounts),calculatedSubtotal=amounts.reduce((sum,row)=>sum+row.subtotal,0),calculatedFee=amounts.reduce((sum,row)=>sum+row.filletFee,0),subtotal=invoice?Number(invoice.subtotal)||0:calculatedSubtotal,filletFee=invoice?Number(invoice.fillet_fee)||0:calculatedFee,previous=invoice?Number(invoice.previous_balance)||0:Number(delivery.previousBalance)||0,received=invoice?Number(invoice.payment_received)||0:0,remaining=invoice?Number(invoice.remaining_balance)||0:previous+subtotal+filletFee-received,itemSummary=(delivery.items||[]).map(item=>`${item.species||"품목"} ${qtyText(item.requestedQty)}${item.unit||"개"}`).join(" · ");rows.push({key:sourceKey,source:"delivery",driverId:snapshot.driver_id,deliveryId:delivery.id,driver,routeOrder:Number(delivery.routeOrder)||0,vehicle_number:invoice?.vehicle_number||vehicle,store_id:invoice?.store_id||store?.id||null,store_name:invoice?.store_name||storeName,invoice_no:invoice?.invoice_no||`AUTO-${workDate.replaceAll("-","")}-${String(vehicle||0).padStart(3,"0")}-${String(Number(delivery.routeOrder)||1).padStart(3,"0")}`,subtotal,fillet_fee:filletFee,previous_balance:previous,payment_received:received,remaining_balance:remaining,status:invoice?.status||"unissued",revision_no:invoice?.revision_no||0,invoiceId:invoice?.id||"",invoice,detail:invoice?.detail||{sourceDeliveryId:delivery.id,sourceDriverId:snapshot.driver_id,items:delivery.items||[]},itemSummary:itemSummary||"품목 없음"})})});latest.filter(invoice=>!used.has(invoice.id)).forEach(invoice=>rows.push({key:`invoice:${invoice.id}`,source:"manual",driverId:invoice.driver_id||"",deliveryId:invoice.detail?.sourceDeliveryId||"",driver:data.profiles.find(row=>row.id===invoice.driver_id)?.full_name||"직접 등록",vehicle_number:invoice.vehicle_number,store_id:invoice.store_id,store_name:invoice.store_name,invoice_no:invoice.invoice_no,subtotal:Number(invoice.subtotal)||0,fillet_fee:Number(invoice.fillet_fee)||0,previous_balance:Number(invoice.previous_balance)||0,payment_received:Number(invoice.payment_received)||0,remaining_balance:Number(invoice.remaining_balance)||0,status:invoice.status,revision_no:invoice.revision_no,invoiceId:invoice.id,invoice,detail:invoice.detail||{},itemSummary:invoice.detail?.itemSummary||"직접 등록 자료"}));return rows.sort((a,b)=>(Number(a.vehicle_number)||999)-(Number(b.vehicle_number)||999)||(Number(a.routeOrder)||999)-(Number(b.routeOrder)||999))}
    function renderAccounting(){const accountingRows=operationalAccountingRows(),valid=accountingRows.filter(row=>row.status!=="cancelled"),sales=valid.reduce((sum,row)=>sum+Number(row.subtotal)+Number(row.fillet_fee),0),prev=valid.reduce((sum,row)=>sum+Number(row.previous_balance),0),received=valid.reduce((sum,row)=>sum+Number(row.payment_received),0),remain=valid.reduce((sum,row)=>sum+Number(row.remaining_balance),0),unissued=accountingRows.filter(row=>row.status==="unissued").length,statusLabel={unissued:"배송자료·미발행",draft:"임시",issued:"발행",revised:"수정",cancelled:"취소"};const rows=accountingRows.map(row=>`<tr><td><strong>${esc(row.invoice_no)}</strong><br><small>${row.revision_no?`${row.revision_no}차`:"자동 생성 예정"}</small></td><td>${esc(row.store_name)}<br><small>${esc(row.itemSummary)}</small></td><td>${row.vehicle_number||"-"}호<br><small>${esc(row.driver||"")}</small></td><td class="money">${money(Number(row.subtotal)+Number(row.fillet_fee))}원<br><small>품목 ${money(row.subtotal)} · 작업 ${money(row.fillet_fee)}</small></td><td class="money">${money(row.previous_balance)}원</td><td class="money">${money(row.payment_received)}원</td><td class="money">${money(row.remaining_balance)}원</td><td><span class="badge ${row.status==="cancelled"?"red":row.status==="unissued"?"amber":"green"}">${statusLabel[row.status]||row.status}</span></td><td><div class="row-actions">${can("accounting_write")?(row.invoiceId?`<button class="mini" data-action="invoice-revise" data-id="${row.invoiceId}">직접 수정</button><button class="mini" data-action="invoice-cancel" data-id="${row.invoiceId}" ${row.status==="cancelled"?"disabled":""}>취소</button>`:`<button class="mini" data-action="invoice-source-edit" data-key="${esc(row.key)}">회계 등록·수정</button>`):""}</div></td></tr>`).join("");const history=data.invoices.sort((a,b)=>new Date(b.created_at)-new Date(a.created_at)).map(invoice=>`<tr><td>${fmt(invoice.created_at)}</td><td>${esc(invoice.invoice_no)} · ${invoice.revision_no}차</td><td>${esc(invoice.store_name)}</td><td><span class="badge ${invoice.status==="cancelled"?"red":"green"}">${statusLabel[invoice.status]||invoice.status}</span></td><td>${esc(invoice.revision_reason||"최초 등록")}</td></tr>`).join("");$("#content").innerHTML=pageHead("회계·보고",`${workDate} 배송 이력을 자동으로 불러오고 금액·전미수·수금을 직접 확정합니다.`,`<button class="btn purple" data-action="export-excel">Excel 저장</button>${can("accounting_write")?`<button class="btn primary" data-action="invoice-new">＋ 직접 거래명세</button>`:""}`)+metrics([["배송자료 매출",money(sales)+"원"],["전미수",money(prev)+"원"],["당일 수금",money(received)+"원"],["남은 미수",money(remain)+"원",remain?"warn":""],["회계 미발행",unissued+"건",unissued?"warn":""],["수정·취소 이력",data.invoices.filter(row=>row.revision_no>1||row.status==="cancelled").length]])+panel("배송 이력·거래명세 관리","선택 날짜의 배송원 자료가 자동 표시됩니다. 미발행 자료는 회계 금액을 확인한 뒤 직접 저장하세요.",`<div class="table-scroll" style="display:block"><table class="table"><thead><tr><th>명세번호</th><th>거래처·품목</th><th>호차</th><th>당일금액</th><th>전미수</th><th>수금</th><th>남은 미수</th><th>상태</th><th>관리</th></tr></thead><tbody>${rows||`<tr><td colspan="9" class="empty">${workDate} 배송·거래명세 자료가 없습니다.</td></tr>`}</tbody></table></div>`)+panel("수정·취소 이력","기존 기록을 덮어쓰지 않고 차수와 수정 사유를 보존합니다.",`<div class="table-scroll" style="display:block"><table class="table"><thead><tr><th>시간</th><th>명세번호</th><th>거래처</th><th>상태</th><th>사유</th></tr></thead><tbody>${history||`<tr><td colspan="5" class="empty">선택 날짜의 회계 수정 이력이 없습니다.</td></tr>`}</tbody></table></div>`)}
    function renderAccess(){const staff=data.profiles.filter(x=>x.role!=="driver"),highest=can("role_manage"),rows=staff.map(x=>{const menus=menuIdsFor(x).filter(id=>id!=="access"||["admin","super_admin"].includes(x.role)),isSelf=x.id===me.id,actions=highest?(isSelf?`<button class="mini" data-action="staff-edit" data-id="${x.id}">내 계정 설정</button>`:`<button class="mini" data-action="staff-edit" data-id="${x.id}">권한·메뉴 수정</button><button class="mini" data-action="staff-delete" data-id="${x.id}" style="color:var(--red);border-color:#efb9bd">삭제</button>`):"";return `<tr><td><div class="name"><span class="avatar">${esc(x.full_name.slice(-2))}</span><span><strong>${esc(x.full_name)}</strong><small>${esc(x.employee_code)}</small></span></div></td><td>${roleBadge(x.role)}</td><td><div class="menu-badges">${menus.map(id=>`<span class="badge">${esc(menuLabel(id))}</span>`).join("")||`<span class="badge red">메뉴 없음</span>`}</div><small>${Array.isArray(x.menu_permissions)?"직접 설정":"역할 추천 적용"}</small></td><td><span class="badge ${x.active?"green":"red"}">${x.active?"사용 중":"정지"}</span></td><td><div class="row-actions">${actions}</div></td></tr>`}).join("");const logs=data.logs.slice(0,100).map(x=>`<tr><td>${fmt(x.created_at)}</td><td>${esc(data.profiles.find(p=>p.id===x.actor_id)?.full_name||"시스템")}</td><td>${esc(x.action)}</td><td>${esc(x.entity_type||"")}</td><td class="audit-detail" title="${esc(JSON.stringify(x.detail||{}))}">${esc(JSON.stringify(x.detail||{}))}</td></tr>`).join("");const matrix=[["최고 관리자","본인 설정 · 모든 관리자 생성·수정·삭제 가능"],["배차 관리자","관제·출고·매입·배송원·거래처·긴급 추천"],["회계 관리자","관제·출고·매입·품목·회계 추천"],["현장 관리자","관제·출고·매입·배송원·거래처·긴급 추천"],["조회 전용","권한·감사를 제외한 업무 메뉴 조회 추천"]].map(x=>`<article class="role-card"><h3>${x[0]}</h3><p>${x[1]}</p></article>`).join("");$("#content").innerHTML=pageHead("관리자 권한·감사 기록","최고 관리자만 다른 관리자의 역할·메뉴를 수정하거나 계정을 삭제할 수 있습니다.",highest?`<button class="btn primary" data-action="staff-new">＋ 관리자</button>`:"")+panel("역할별 추천 메뉴","관리자 생성·변경 화면에서 추천 메뉴가 자동 체크되며 최고 관리자가 자유롭게 조정할 수 있습니다.",`<div class="role-matrix">${matrix}</div>`)+panel("관리자 계정","본인은 ‘내 계정 설정’에서 이름·아이디·메뉴를 수정할 수 있습니다. 다른 관리자는 최고 관리자만 수정·삭제할 수 있습니다.",`<div class="table-scroll" style="display:block"><table class="table"><thead><tr><th>담당자</th><th>역할</th><th>허용 메뉴</th><th>상태</th><th>관리</th></tr></thead><tbody>${rows}</tbody></table></div>`)+panel("최근 감사 기록","관리자 생성·수정·삭제와 메뉴 권한 변경 내용이 기록됩니다.",`<div class="table-scroll" style="display:block"><table class="table"><thead><tr><th>시간</th><th>담당자</th><th>행동</th><th>대상</th><th>내용</th></tr></thead><tbody>${logs||`<tr><td colspan="5" class="empty">기록이 없습니다.</td></tr>`}</tbody></table></div>`);}
    function modal(title,desc,body,wide=false){$("#modal").innerHTML=`<div class="modal-card ${wide?"wide":""}"><header class="modal-head"><div><h2>${title}</h2><p>${desc}</p></div><button class="close" data-action="modal-close" type="button">×</button></header>${body}</div>`;$("#modal").classList.remove("hidden")}
    const highestAdmin=()=>["admin","super_admin"].includes(me?.role);
    const localDateTimeInput=value=>{if(!value)return"";const date=new Date(value);if(Number.isNaN(date.getTime()))return"";return new Date(date.getTime()-date.getTimezoneOffset()*60000).toISOString().slice(0,16)};
    const safeId=()=>Date.now()+Math.floor(Math.random()*100000);
    const tankText=value=>Number(value)>0?`${Number(value)}번 수조`:"박스 운반";
    const weightTotal=item=>Number((Array.isArray(item?.weighings)?item.weighings:[]).reduce((sum,row)=>sum+(Number(row.weight)||0),0).toFixed(3));
    function driverWorkday(id){
      const driver=drivers().find(row=>row.id===id),snapshot=data.snapshots.find(row=>row.driver_id===id),payload=snapshot?.payload||{},customers=Array.isArray(payload.customers)?payload.customers:[],deliveries=(Array.isArray(payload.deliveries)?payload.deliveries:[]).filter(row=>dateKey(row.deliveryDate)===dateKey(workDate)).sort((a,b)=>(Number(a.routeOrder)||0)-(Number(b.routeOrder)||0));
      return{driver,snapshot,payload,customers,deliveries,status:statusOf(id),completion:payload.workClosures?.[workDate]||null};
    }
    function storeNameFor(work,delivery){return work.customers.find(row=>String(row.id)===String(delivery.customerId))?.name||delivery.fallbackStoreName||"거래처 미확인"}
    function workdayItemDetail(item){
      const weighings=Array.isArray(item.weighings)?item.weighings:[],measured=weightTotal(item),manual=Number(item.actualWeight)||0,weight=measured||manual,workflow=weighings.length?`${weighings.filter(row=>row.calledAt).length}/${weighings.length} 콜 · ${weighings.filter(row=>row.repeatConfirmedAt).length}/${weighings.length} 복창 · ${weighings.filter(row=>row.bandVerifiedAt).length}/${weighings.length} 밴드 · ${weighings.filter(row=>row.loadedAt).length}/${weighings.length} 상차`:(item.tankLoaded?"상차 완료":"상차 확인 전");
      return `<div class="detail-line"><strong>${esc(item.species||"품목명 미입력")}<br><small>${esc(item.category||"기타")} · ${esc(item.size||"규격 없음")} · ${esc(item.originCountry||"원산지 미입력")}</small></strong><span><b>발주</b><br>${qtyText(item.requestedQty)}${esc(item.unit||"개")}</span><span><b>계근·중량</b><br>${weight?kgText(weight):"-"}${weighings.length?` · ${weighings.length}마리`:""}</span><span><b>운반·상차</b><br>${esc(tankText(item.tankNumber))} · ${Number(item.tankNumber)>0?(item.tankLoaded?"완료":"확인 전"):"박스"}</span><span><b>작업</b><br>${esc(item.processing||"-")}${item.rubberBand?` · 밴드 ${esc(item.rubberBandColor||"색상 미입력")} ${Number(item.rubberBandQuantity)||0}개`:""}</span></div>${weighings.length?`<div class="weigh-chips">${weighings.map((row,index)=>`<span>${index+1}번 ${Number(row.weight||0).toFixed(3)}kg · ${row.loadedAt?"상차 완료":"상차 확인 전"}</span>`).join("")}</div><div class="weigh-chips"><span>현장 단계 ${esc(workflow)}</span><span>계근 ${item.weighingConfirmed?"확정":"미확정"}</span>${item.includedNote?`<span>메모 ${esc(item.includedNote)}</span>`:""}</div>`:item.includedNote?`<div class="weigh-chips"><span>메모 ${esc(item.includedNote)}</span></div>`:""}`;
    }
    function showDriverWorkdayDetails(id){
      const work=driverWorkday(id);if(!work.driver)return toast("배송원 정보를 찾지 못했습니다.",true);
      const active=work.deliveries.filter(row=>row.status!=="cancelled"),items=active.flatMap(row=>row.items||[]),boxes=active.flatMap(row=>row.boxes||[]),tankItems=items.filter(row=>Number(row.tankNumber)>0),loaded=tankItems.filter(row=>row.tankLoaded).length,problem=work.completion?.result==="issue",departure=data.departures.find(row=>row.driver_id===id)||{},checks=[["발주",departure.orders_checked],["수조",departure.tanks_checked||tankItems.length===loaded],["박스",departure.boxes_checked||work.status.box_total===work.status.box_checked],["긴급",departure.urgent_checked||!work.status.urgent_pending],["인계",departure.handover_checked]];
      const deliveryHtml=work.deliveries.map(delivery=>{const deliveryItems=delivery.items||[],deliveryBoxes=delivery.boxes||[],boxSummary=deliveryBoxes.map(box=>`${box.boxNumber||"-"}번 ${box.contents||"내용 미입력"} ${box.checked?"✓":"○"}`).join(" · ");return `<article class="delivery-detail ${delivery.status==="cancelled"?"cancelled":""}"><div class="delivery-detail-head"><div><h3>${Number(delivery.routeOrder)||"-"}번 · ${esc(storeNameFor(work,delivery))}</h3><p>전미수 ${money(delivery.previousBalance)}원 · 계획 ${deliveryBoxes.length}박스 / 실제 ${Number(delivery.loadedBoxCount)||0}박스</p></div><span class="badge ${delivery.status==="cancelled"?"red":delivery.boxCountConfirmed?"green":""}">${delivery.status==="cancelled"?"당일 제외":delivery.boxCountConfirmed?"박스 확인":"배송 배정"}</span></div><div class="detail-section"><h4>품목·계근·상차</h4>${deliveryItems.map(workdayItemDetail).join("")||`<div class="empty">등록된 품목이 없습니다.</div>`}</div><div class="detail-section"><h4>박스·동봉 내용</h4><p>${esc(boxSummary||"등록된 박스 없음")}</p>${delivery.coPackNote?`<p><b>동봉 메모:</b> ${esc(delivery.coPackNote)}</p>`:""}${delivery.notes?`<p><b>전체 메모:</b> ${esc(delivery.notes)}</p>`:""}</div></article>`}).join("");
      const audit=data.logs.filter(row=>row.action==="driver_workday_updated"&&String(row.entity_id)===String(id)&&row.detail?.workDate===workDate).slice(0,10).map(row=>`<div class="audit-row"><b>${fmt(row.created_at)} · ${esc(data.profiles.find(profile=>profile.id===row.actor_id)?.full_name||"관리자")}</b><br>${esc(row.detail?.reason||"수정 사유 미입력")}<br><small>배송 ${row.detail?.before?.deliveries??"-"}→${row.detail?.after?.deliveries??"-"}곳 · 품목 ${row.detail?.before?.items??"-"}→${row.detail?.after?.items??"-"}개 · 박스 ${row.detail?.before?.boxes??"-"}→${row.detail?.after?.boxes??"-"}개</small></div>`).join("");
      modal(`${work.driver.vehicle_number||"-"}호 · ${esc(work.driver.full_name)}`,"당일 배송·상차·계근·박스·완료 기록을 확인합니다.",`<div class="modal-body"><div class="workday-toolbar">${highestAdmin()?`<button class="btn primary" data-action="workday-edit" data-id="${id}" type="button">당일 내용 전체 수정</button>${departure.approved?`<button class="btn warn" data-action="departure-reset" data-id="${id}" type="button">출차 승인 취소</button>`:""}`:""}</div>${metrics([["출차 승인",departure.approved?"승인":"미승인",departure.approved?"":"warn"],["전체 배송",work.completion?(problem?"확인 필요":"완료"):"진행 중",problem?"bad":""],["배정 점포",`${active.length}곳`],["상차 품목",`${loaded}/${tankItems.length}`],["박스",`${boxes.filter(box=>box.checked).length}/${boxes.length}`],["마지막 동기화",fmt(work.snapshot?.updated_at||work.status.last_sync_at)]])}<div class="checks">${checks.map(row=>`<span class="check ${row[1]?"ok":""}">${row[1]?"✓":"○"} ${row[0]}</span>`).join("")}</div>${work.completion?`<div class="notice"><b>전체 완료:</b> ${fmt(work.completion.at)}${work.completion.note?` · ${esc(work.completion.note)}`:""}</div>`:""}${deliveryHtml||`<div class="empty">선택한 날짜의 배송 상세가 없습니다.</div>`}${audit?`<section class="detail-section"><h4>관리자 보정 이력</h4><div class="audit-list">${audit}</div></section>`:""}</div>`,true)
    }
    function editorItemHtml(item={},index=0){
      const id=Number(item.id)||safeId(),weights=(item.weighings||[]).map(row=>Number(row.weight||0).toFixed(3)).join(", ");
      return `<article class="editor-item" data-item-id="${id}"><div class="editor-item-head"><strong>${index+1}번 품목</strong><label class="check-field" style="margin:0;min-height:38px"><input data-item-field="delete" type="checkbox">품목 삭제</label></div><div class="compact-grid"><label class="field"><span>구분</span><select class="control" data-item-field="category">${["활어","패류","냉동제품","필렛 제품","기타"].map(value=>`<option ${value===(item.category||"활어")?"selected":""}>${value}</option>`).join("")}</select></label><label class="field"><span>어종·품목</span><input class="control" data-item-field="species" value="${esc(item.species||"")}"></label><label class="field"><span>규격</span><input class="control" data-item-field="size" value="${esc(item.size||"")}"></label><label class="field"><span>원산지</span><input class="control" data-item-field="originCountry" value="${esc(item.originCountry||"")}"></label><label class="field"><span>발주 수량</span><input class="control" data-item-field="requestedQty" type="number" min="0" step=".001" value="${Number(item.requestedQty)||0}"></label><label class="field"><span>단위</span><select class="control" data-item-field="unit">${["kg","마리","팩","박스","망","봉지","개"].map(value=>`<option ${value===(item.unit||"마리")?"selected":""}>${value}</option>`).join("")}</select></label><label class="field"><span>실중량 kg</span><input class="control" data-item-field="actualWeight" type="number" min="0" step=".001" value="${item.actualWeight??""}"></label><label class="field"><span>개별 계근 kg · 쉼표 구분</span><input class="control" data-item-field="weights" value="${esc(weights)}" placeholder="1.250, 1.330"></label><label class="field"><span>판매 단가</span><input class="control" data-item-field="unitPrice" type="number" min="0" step="1" value="${Number(item.unitPrice)||0}"></label><label class="field"><span>가격 기준</span><select class="control" data-item-field="priceUnit">${["kg","마리","팩","박스","망","봉지","개"].map(value=>`<option ${value===(item.priceUnit||item.unit||"kg")?"selected":""}>${value}당</option>`).join("")}</select></label><label class="field"><span>필렛 작업비/kg</span><input class="control" data-item-field="processingFeePerKg" type="number" min="0" step="1" value="${Number(item.processingFeePerKg)||0}"></label><label class="field"><span>작업·보관</span><input class="control" data-item-field="processing" value="${esc(item.processing||"")}"></label><label class="field"><span>운반 위치</span><select class="control" data-item-field="tankNumber"><option value="0" ${!Number(item.tankNumber)?"selected":""}>박스 운반</option>${[1,2,3,4,5,6].map(value=>`<option value="${value}" ${Number(item.tankNumber)===value?"selected":""}>${value}번 수조</option>`).join("")}</select></label><label class="field"><span>밴드 색상</span><input class="control" data-item-field="rubberBandColor" value="${esc(item.rubberBandColor||"")}"></label><label class="field"><span>밴드 수량</span><input class="control" data-item-field="rubberBandQuantity" type="number" min="0" value="${Number(item.rubberBandQuantity)||0}"></label><label class="field"><span>품목 메모</span><input class="control" data-item-field="includedNote" value="${esc(item.includedNote||"")}"></label></div><div class="compact-grid"><label class="check-field"><input data-item-field="weighingConfirmed" type="checkbox" ${item.weighingConfirmed?"checked":""}>계근 확정</label><label class="check-field"><input data-item-field="tankLoaded" type="checkbox" ${item.tankLoaded?"checked":""}>실제 상차 완료</label><label class="check-field"><input data-item-field="rubberBand" type="checkbox" ${item.rubberBand?"checked":""}>고무밴드 작업</label><label class="check-field"><input data-item-field="checked" type="checkbox" ${item.checked?"checked":""}>품목 확인</label></div></article>`;
    }
    function editorBoxHtml(box={},index=0){
      return `<article class="editor-box" data-box-id="${Number(box.id)||safeId()}"><div class="compact-grid"><label class="field wide-field"><span>${index+1}번 박스 내용</span><input class="control" data-box-field="contents" value="${esc(box.contents||"")}"></label><label class="check-field"><input data-box-field="checked" type="checkbox" ${box.checked?"checked":""}>실물 확인</label><label class="check-field"><input data-box-field="delete" type="checkbox">박스 삭제</label></div></article>`;
    }
    function editorDeliveryHtml(work,delivery={},index=0){
      const customerId=Number(delivery.customerId)||0,items=Array.isArray(delivery.items)?delivery.items:[],boxes=Array.isArray(delivery.boxes)?delivery.boxes:[];
      return `<section class="editor-delivery" data-delivery-id="${Number(delivery.id)||safeId()}" data-customer-id="${customerId}"><div class="editor-delivery-head"><h3>${index+1}번째 배송지</h3><label class="check-field" style="margin:0"><input data-delivery-field="cancelled" type="checkbox" ${delivery.status==="cancelled"?"checked":""}>당일 배정 제외</label></div><div class="compact-grid"><label class="field"><span>배송 순서</span><input class="control" data-delivery-field="routeOrder" type="number" min="1" value="${Number(delivery.routeOrder)||index+1}"></label><label class="field"><span>거래처명</span><input class="control" data-delivery-field="storeName" value="${esc(storeNameFor(work,delivery))}"></label><label class="field"><span>전미수</span><input class="control" data-delivery-field="previousBalance" type="number" min="0" value="${Number(delivery.previousBalance)||0}"></label><label class="field"><span>실제 상차 박스</span><input class="control" data-delivery-field="loadedBoxCount" type="number" min="0" value="${Number(delivery.loadedBoxCount)||0}"></label><label class="check-field"><input data-delivery-field="boxCountConfirmed" type="checkbox" ${delivery.boxCountConfirmed?"checked":""}>박스 수량 대조</label><label class="check-field"><input data-delivery-field="isUrgentAdditional" type="checkbox" ${delivery.isUrgentAdditional?"checked":""}>긴급 추가 발주</label><label class="field wide-field"><span>동봉 메모</span><input class="control" data-delivery-field="coPackNote" value="${esc(delivery.coPackNote||"")}"></label><label class="field wide-field"><span>전체 메모</span><textarea class="control" data-delivery-field="notes">${esc(delivery.notes||"")}</textarea></label></div><div class="subhead"><h4>품목</h4><button class="mini" data-action="workday-add-item" type="button">＋ 품목 추가</button></div><div class="editor-items">${items.map(editorItemHtml).join("")||editorItemHtml({},0)}</div><div class="subhead"><h4>박스·동봉품</h4><button class="mini" data-action="workday-add-box" type="button">＋ 박스 추가</button></div><div class="editor-boxes">${boxes.map(editorBoxHtml).join("")}</div></section>`;
    }
    function showDriverWorkdayEditor(id){
      if(!highestAdmin())return toast("최고 관리자만 수정할 수 있습니다.",true);
      const work=driverWorkday(id);if(!work.snapshot?.payload)return toast("배송원 중앙 저장 자료가 없습니다.",true);
      const completion=work.completion||{},deliveries=work.deliveries.length?work.deliveries:[{id:safeId(),customerId:0,deliveryDate:workDate,routeOrder:1,status:"ready",items:[],boxes:[]}];
      modal(`${work.driver.vehicle_number||"-"}호 · ${esc(work.driver.full_name)} 당일 자료 수정`,"최고 관리자가 배송·상차 자료를 보정합니다. 저장 사유와 변경 전후 수량이 감사 기록에 남습니다.",`<form id="workdayEditorForm" class="modal-body" data-driver-id="${id}"><div class="notice">현장 사실과 확인된 내용만 수정해 주세요. 저장하면 배송원 웹앱이 다음 중앙 동기화 때 변경 내용을 받아옵니다.</div><div class="subhead"><h4>${workDate} 배송·상차</h4><button class="btn purple" data-action="workday-add-delivery" type="button">＋ 배송지 추가</button></div><div id="workdayDeliveries">${deliveries.map((delivery,index)=>editorDeliveryHtml(work,delivery,index)).join("")}</div><section class="completion-editor"><label class="check-field" style="margin:0"><input name="completionEnabled" type="checkbox" ${work.completion?"checked":""}>당일 전체 배송 완료 기록</label><div class="compact-grid"><label class="field"><span>완료 결과</span><select class="control" name="completionResult"><option value="normal" ${completion.result!=="issue"?"selected":""}>문제 없이 완료</option><option value="issue" ${completion.result==="issue"?"selected":""}>관리자 확인 필요</option></select></label><label class="field"><span>전체 완료 시간</span><input class="control" name="completionAt" type="datetime-local" value="${localDateTimeInput(completion.at||new Date())}"></label><label class="field"><span>긴급 미처리</span><input class="control" name="urgentPending" type="number" min="0" value="${Number(completion.urgentPending??work.status.urgent_pending)||0}"></label><label class="field"><span>미해결 오류</span><input class="control" name="issueCount" type="number" min="0" value="${Number(completion.issueCount??work.status.issue_count)||0}"></label><label class="field wide-field"><span>완료 메모</span><textarea class="control" name="completionNote">${esc(completion.note||"")}</textarea></label></div></section><label class="field"><span>수정 사유 · 필수</span><textarea class="control" name="reason" required placeholder="예: 현장 확인 후 2번 수조를 박스 운반으로 정정">${""}</textarea></label><div id="formMessage" class="message"></div><div class="form-actions"><button class="btn" data-action="driver-details" data-id="${id}" type="button">수정 취소</button><button class="btn primary" type="submit">전체 내용 저장</button></div></form>`,true)
    }
    function collectDriverWorkday(form){
      const work=driverWorkday(form.dataset.driverId),originals=new Map(work.deliveries.map(row=>[String(row.id),row])),customerNames={};
      const deliveries=[...form.querySelectorAll(".editor-delivery")].map((section,deliveryIndex)=>{const get=name=>section.querySelector(`[data-delivery-field="${name}"]`),id=String(section.dataset.deliveryId),original=JSON.parse(JSON.stringify(originals.get(id)||{})),customerId=Number(section.dataset.customerId)||0,storeName=String(get("storeName")?.value||"").trim();if(customerId)customerNames[String(customerId)]=storeName;
        const items=[...section.querySelectorAll(".editor-item")].filter(node=>!node.querySelector('[data-item-field="delete"]')?.checked).map(node=>{const itemGet=name=>node.querySelector(`[data-item-field="${name}"]`),source=(original.items||[]).find(row=>String(row.id)===String(node.dataset.itemId))||{},weights=String(itemGet("weights")?.value||"").split(/[,\n]/).map(value=>Number(value.trim())).filter(value=>Number.isFinite(value)&&value>0),oldWeighings=Array.isArray(source.weighings)?source.weighings:[],weighings=weights.map((weight,index)=>({...oldWeighings[index],id:Number(oldWeighings[index]?.id)||safeId()+index,weight,at:oldWeighings[index]?.at||new Date().toISOString()}));return{...source,id:Number(node.dataset.itemId)||safeId(),category:itemGet("category").value,species:itemGet("species").value,size:itemGet("size").value,originCountry:itemGet("originCountry").value,requestedQty:Number(itemGet("requestedQty").value)||0,unit:itemGet("unit").value,actualWeight:itemGet("actualWeight").value===""?null:Number(itemGet("actualWeight").value)||0,weighings,weighingConfirmed:itemGet("weighingConfirmed").checked,unitPrice:Number(itemGet("unitPrice").value)||0,priceUnit:itemGet("priceUnit").value,processingFeePerKg:Number(itemGet("processingFeePerKg").value)||0,processing:itemGet("processing").value,tankNumber:Number(itemGet("tankNumber").value)||0,tankLoaded:itemGet("tankLoaded").checked,rubberBand:itemGet("rubberBand").checked,rubberBandColor:itemGet("rubberBandColor").value,rubberBandQuantity:Number(itemGet("rubberBandQuantity").value)||0,includedNote:itemGet("includedNote").value,checked:itemGet("checked").checked}});
        const boxes=[...section.querySelectorAll(".editor-box")].filter(node=>!node.querySelector('[data-box-field="delete"]')?.checked).map((node,index)=>{const source=(original.boxes||[]).find(row=>String(row.id)===String(node.dataset.boxId))||{};return{...source,id:Number(node.dataset.boxId)||safeId()+index,boxNumber:index+1,contents:node.querySelector('[data-box-field="contents"]').value,checked:node.querySelector('[data-box-field="checked"]').checked}});
        return{...original,id:Number(id)||safeId()+deliveryIndex,customerId,fallbackStoreName:storeName,deliveryDate:workDate,routeOrder:Number(get("routeOrder").value)||deliveryIndex+1,status:get("cancelled").checked?"cancelled":"ready",previousBalance:Number(get("previousBalance").value)||0,loadedBoxCount:Number(get("loadedBoxCount").value)||0,boxCountConfirmed:get("boxCountConfirmed").checked,isUrgentAdditional:get("isUrgentAdditional").checked,coPackNote:get("coPackNote").value,notes:get("notes").value,items,boxes}
      });
      const fd=new FormData(form),completion={enabled:fd.get("completionEnabled")==="on",result:fd.get("completionResult"),at:fd.get("completionAt")?new Date(fd.get("completionAt")).toISOString():new Date().toISOString(),urgentPending:Number(fd.get("urgentPending"))||0,issueCount:Number(fd.get("issueCount"))||0,note:String(fd.get("completionNote")||""),by:work.driver.full_name,vehicle:String(work.driver.vehicle_number||"")};
      return{driverId:form.dataset.driverId,workDate,deliveries,customerNames,completion,reason:String(fd.get("reason")||"").trim()}
    }
    const field=(label,name,value="",type="text",wide=false,extra="")=>`<label class="field ${wide?"wide-field":""}"><span>${label}</span><input class="control" name="${name}" type="${type}" value="${esc(value)}" ${extra}></label>`;
    function driverForm(d=null){const canChangeLoginId=can("role_manage"),idRule=d&&!canChangeLoginId?"readonly":'pattern="[a-z0-9_-]{3,24}" required',desc=d?(canChangeLoginId?"배송원 아이디·호차·이름·연락처·상태를 모두 수정할 수 있습니다. 아이디 변경 후에는 새 아이디로 로그인합니다.":"호차·이름·연락처·상태를 수정합니다. 로그인 아이디는 최고 관리자만 변경할 수 있습니다."):"아이디와 임시 비밀번호를 배송원에게 전달합니다.";modal(d?"배송원 전체 정보 수정":"새 배송원 등록",desc,`<form id="driverForm" class="modal-body" data-id="${d?.id||""}"><div class="form-grid">${field("배송원 로그인 아이디","employeeCode",d?.employee_code||"","text",false,idRule)}${field("배정 호차","vehicleNumber",d?.vehicle_number||"","number",false,"min=\"1\" max=\"999\" required")}${field("배송원 이름","fullName",d?.full_name||"","text",true,"required")}${field("연락처","phone",d?.phone||"","tel",true)}${d?`<label class="field wide-field"><span>계정 상태</span><select class="control" name="active"><option value="true" ${d.active?"selected":""}>사용 중</option><option value="false" ${!d.active?"selected":""}>퇴사·사용 정지</option></select></label>${field("아이디·호차 변경 또는 퇴사 사유","note","","text",true)}`:field("임시 비밀번호 · 8자리 이상","password","","text",true,"minlength=\"8\" required")}</div>${d&&canChangeLoginId?`<div class="notice">배송원 아이디를 변경하면 기존 아이디로는 로그인할 수 없습니다. 비밀번호는 그대로 유지되며 새 아이디를 배송원에게 전달해야 합니다.</div>`:""}<div id="formMessage" class="message"></div><div class="form-actions">${d?`<button class="btn" data-action="password-reset" type="button">비밀번호 초기화</button>`:`<button class="btn" data-action="modal-close" type="button">취소</button>`}<button class="btn primary" type="submit">${d?"전체 정보 저장":"계정 생성"}</button></div></form>`)}
    function storeForm(s=null){const opts=drivers().filter(x=>x.active).map(d=>`<option value="${d.id}" ${s?.assigned_driver_id===d.id?"selected":""}>${d.vehicle_number}호 · ${esc(d.full_name)}</option>`).join("");modal(s?"거래처·인수인계 수정":"새 거래처 등록","배송 교체 시 그대로 전달할 표준 정보를 입력합니다.",`<form id="storeForm" class="modal-body" data-id="${s?.id||""}"><div class="form-grid">${field("거래처 코드","store_code",s?.store_code||"","text",false,"required")}${field("거래처명","name",s?.name||"","text",false,"required")}${field("주소","address",s?.address||"","text",true)}${field("연락처","phone",s?.phone||"","tel")}${field("담당자","contact_name",s?.contact_name||"") }<label class="field"><span>배정 배송원</span><select class="control" name="assigned_driver_id"><option value="">미배정</option>${opts}</select></label>${field("배송 순서","delivery_order",s?.delivery_order||"","number",false,"min=\"1\" max=\"999\"")}${field("매장 입고 시간","preferred_time",s?.preferred_time||"")}${field("매장 진입 방법","entry_method",s?.entry_method||"","text",true)}${field("물건 놓는 자리","placement",s?.placement||"","text",true)}${field("하지 말아야 할 사항","prohibited",s?.prohibited||"","text",true)}${field("주차·하차 참고","parking_note",s?.parking_note||"","text",true)}<label class="field wide-field"><span>기타 인수인계</span><textarea class="control" name="handover_note">${esc(s?.handover_note||"")}</textarea></label><label class="field wide-field"><span>거래 상태</span><select class="control" name="active"><option value="true" ${s?.active!==false?"selected":""}>거래 중</option><option value="false" ${s?.active===false?"selected":""}>거래 중지</option></select></label></div><div id="formMessage" class="message"></div><div class="form-actions"><button class="btn" data-action="modal-close" type="button">취소</button><button class="btn primary" type="submit">저장</button></div></form>`,true)}
    function urgentForm(x=null){const stores=data.stores.filter(s=>s.active).map(s=>`<option value="${s.id}" data-name="${esc(s.name)}" data-vehicle="${s.assigned_vehicle||""}" ${x?.store_id===s.id?"selected":""}>${s.assigned_vehicle||"-"}호 · ${esc(s.name)}</option>`).join("");modal(x?"긴급 발주 수정":"긴급 추가 발주 등록","저장 즉시 배정 배송원 화면에 표시됩니다.",`<form id="urgentForm" class="modal-body" data-id="${x?.id||""}"><div class="form-grid"><label class="field wide-field"><span>거래처</span><select class="control" name="store_id" required><option value="">선택</option>${stores}</select></label>${field("배정 호차","assigned_vehicle",x?.assigned_vehicle||"","number",false,"min=\"1\" max=\"999\" required")}<label class="field"><span>긴급도</span><select class="control" name="priority"><option value="urgent">긴급</option><option value="critical" ${x?.priority==="critical"?"selected":""}>최우선</option><option value="normal" ${x?.priority==="normal"?"selected":""}>일반</option></select></label>${field("품목 내용","item_summary",x?.item_summary||"","text",true,"required")}${field("수량·단위","quantity_summary",x?.quantity_summary||"","text",true)}<label class="field wide-field"><span>작업·배송 메모</span><textarea class="control" name="note">${esc(x?.note||"")}</textarea></label></div><div id="formMessage" class="message"></div><div class="form-actions"><button class="btn" data-action="modal-close" type="button">취소</button><button class="btn primary" type="submit">긴급 발주 전송</button></div></form>`)}
    function productForm(p=null){modal(p?"품목 수정":"새 품목 등록","포장 단위와 원산지·필렛 작업비를 중앙에서 통일합니다.",`<form id="productForm" class="modal-body" data-id="${p?.id||""}"><div class="form-grid">${field("품목 코드","product_code",p?.product_code||"","text",false,"required")}${field("품목명","name",p?.name||"","text",false,"required")}<label class="field"><span>구분</span><select class="control" name="category">${["활어","패류","냉동제품","필렛 제품","기타"].map(x=>`<option ${p?.category===x?"selected":""}>${x}</option>`).join("")}</select></label><label class="field"><span>계산 단위</span><select class="control" name="unit">${["kg","마리","팩","박스","망","봉지","개"].map(x=>`<option ${p?.unit===x?"selected":""}>${x}</option>`).join("")}</select></label>${field("원산지","origin_country",p?.origin_country||"국내산")}${field("기본 단가","base_price",p?.base_price||0,"number",false,"min=\"0\" step=\"1\" required")}${field("필렛 작업비 / 원물 kg","fillet_fee_per_kg",p?.fillet_fee_per_kg||0,"number",false,"min=\"0\" step=\"1\"")}${field("기본 밴드 색상","default_band_color",p?.default_band_color||"")}${field("적용 시작일","effective_from",p?.effective_from||localDate(),"date")}${field("적용 종료일","effective_to",p?.effective_to||"","date")}<label class="field wide-field"><span>사용 상태</span><select class="control" name="active"><option value="true" ${p?.active!==false?"selected":""}>사용</option><option value="false" ${p?.active===false?"selected":""}>중지</option></select></label></div><div id="formMessage" class="message"></div><div class="form-actions"><button class="btn" data-action="modal-close" type="button">취소</button><button class="btn primary" type="submit">품목 저장</button></div></form>`)}
    function invoiceForm(x=null,source=null){
      const base=x||source||{},stores=data.stores.map(s=>`<option value="${s.id}" data-name="${esc(s.name)}" data-vehicle="${s.assigned_vehicle||""}" ${base.store_id===s.id?"selected":""}>${esc(s.name)}</option>`).join(""),isRevision=Boolean(x),sourceDetail=base.detail||{},sourceDeliveryId=source?.deliveryId||sourceDetail.sourceDeliveryId||"",sourceDriverId=source?.driverId||x?.driver_id||sourceDetail.sourceDriverId||"",itemSummary=source?.itemSummary||sourceDetail.itemSummary||"";
      modal(isRevision?"회계 내용 직접 수정":source?"배송자료 회계 확정":"거래명세 직접 등록",isRevision?"기존 기록은 보존하고 수정본을 새 차수로 저장합니다.":source?`${workDate} 배송자료를 불러왔습니다. 모든 항목을 직접 확인하고 수정할 수 있습니다.`:"거래처·금액·전미수·수금액을 직접 기록합니다.",`<form id="invoiceForm" class="modal-body" data-id="${x?.id||""}"><input type="hidden" name="source_delivery_id" value="${esc(sourceDeliveryId)}"><input type="hidden" name="driver_id" value="${esc(sourceDriverId)}"><input type="hidden" name="item_summary" value="${esc(itemSummary)}"><div class="form-grid">${field("거래명세 번호","invoice_no",base.invoice_no||`JB-${workDate.replaceAll("-","")}-${String(data.invoices.length+1).padStart(3,"0")}`,"text",false,"required")}<label class="field"><span>등록 거래처 선택 · 선택 사항</span><select class="control" name="store_id"><option value="">직접 입력</option>${stores}</select></label>${field("거래처명 · 직접 수정 가능","store_name",base.store_name||"","text",true,"required")}${field("호차","vehicle_number",base.vehicle_number||"","number",false,"min=\"1\" max=\"999\"")}${field("품목 금액","subtotal",base.subtotal||0,"number",false,"step=\"1\" required")}${field("필렛 작업비","fillet_fee",base.fillet_fee||0,"number",false,"step=\"1\"")}${field("전미수","previous_balance",base.previous_balance||0,"number",false,"step=\"1\"")}${field("당일 수금","payment_received",base.payment_received||0,"number",false,"step=\"1\"")}${field(isRevision?"수정 사유":"등록·확정 사유","revision_reason",isRevision?"":source?"배송자료 회계 확정":"직접 등록","text",true,"required")}<label class="field wide-field"><span>회계 메모 · 직접 수정 가능</span><textarea class="control" name="accounting_note">${esc(sourceDetail.accountingNote||"")}</textarea></label></div>${itemSummary?`<div class="notice"><b>배송 품목:</b> ${esc(itemSummary)}<br>품목 목록은 원본 배송자료와 연결하여 보존됩니다.</div>`:""}<div class="notice">남은 미수는 ‘전미수 + 품목금액 + 작업비 - 당일수금’으로 자동 계산됩니다.</div><div id="formMessage" class="message"></div><div class="form-actions"><button class="btn" data-action="modal-close" type="button">취소</button><button class="btn primary" type="submit">${isRevision?"수정본 저장":source?"회계 확정 저장":"명세 저장"}</button></div></form>`,true)
    }
    function permissionOptions(role,selected,lockAccess=false){const recommended=roleMenuDefaults[role]||["dashboard"],chosen=Array.isArray(selected)?selected:recommended;return tabs.map(([id,label])=>{const accessLocked=lockAccess&&id==="access",disabled=accessLocked||(id==="access"&&!["admin","super_admin"].includes(role)),checked=accessLocked||chosen.includes(id);return `<label class="menu-option" data-menu="${id}"><input type="checkbox" name="menuPermissions" value="${id}" ${checked?"checked":""} ${disabled?"disabled":""}><span><strong>${esc(label)}</strong><small>${esc(menuDescription[id])}</small></span><em class="recommend-mark ${recommended.includes(id)?"":"hidden"}">추천</em></label>`}).join("")}
    function syncPermissionForm(useRecommended=true){const f=$("#staffForm");if(!f)return;const role=f.role.value,recommended=roleMenuDefaults[role]||["dashboard"],isSuper=["admin","super_admin"].includes(role),lockAccess=f.dataset.self==="true";f.querySelectorAll('input[name="menuPermissions"]').forEach(input=>{const selfAccess=lockAccess&&input.value==="access";input.disabled=selfAccess||(input.value==="access"&&!isSuper);if(selfAccess)input.checked=true;else if(input.disabled)input.checked=false;else if(useRecommended)input.checked=recommended.includes(input.value);const mark=input.closest(".menu-option").querySelector(".recommend-mark");mark.classList.toggle("hidden",!recommended.includes(input.value))});const title=$("#permissionRoleName");if(title)title.textContent=roleLabel[role]||role}
    function staffForm(s=null){
      const isExisting=Boolean(s),isSelf=Boolean(s&&s.id===me.id),role=s?.role==="admin"?"super_admin":s?.role||"dispatch_manager",selected=Array.isArray(s?.menu_permissions)?s.menu_permissions:null;
      const title=isSelf?"내 관리자 계정 설정":isExisting?"관리자 정보·권한 수정":"새 관리자 계정";
      const desc=isSelf?"이름·관리자 아이디·허용 메뉴를 직접 수정합니다. 최고 관리자 역할과 사용 상태는 안전을 위해 고정됩니다.":isExisting?"최고 관리자만 담당자 정보·역할·허용 메뉴·사용 상태를 변경할 수 있습니다.":"역할을 고르면 추천 메뉴 체크가 즉시 바뀝니다. 새 계정은 기존 관리자와 다른 이메일을 사용하세요.";
      const roleField=isSelf?`<label class="field wide-field"><span>역할 · 본인 보호를 위해 변경 불가</span><input class="control" value="${esc(roleLabel[role])}" readonly><input type="hidden" name="role" value="${role}"></label>`:`<label class="field wide-field"><span>역할 · 선택 즉시 아래 추천 메뉴가 체크됩니다</span><select class="control" name="role">${["super_admin","dispatch_manager","accounting_manager","field_manager","viewer"].map(r=>`<option value="${r}" ${role===r?"selected":""}>${roleLabel[r]}</option>`).join("")}</select></label>`;
      const stateField=isSelf?`<label class="field wide-field"><span>계정 상태 · 본인 보호를 위해 변경 불가</span><input class="control" value="사용 중" readonly><input type="hidden" name="active" value="true"></label>`:isExisting?`<label class="field wide-field"><span>계정 상태</span><select class="control" name="active"><option value="true" ${s.active?"selected":""}>사용 중</option><option value="false" ${!s.active?"selected":""}>사용 정지</option></select></label>`:"";
      modal(title,desc,`<form id="staffForm" class="modal-body" data-id="${s?.id||""}" data-self="${isSelf}"><div class="form-grid">${isExisting?"":field("로그인 이메일 · 기존 계정과 중복 불가","email","","email",true,"required")}${field("관리자 아이디","employeeCode",s?.employee_code||"","text",false,"pattern=\"[a-z0-9_-]{3,24}\" required")}${field("담당자 이름","fullName",s?.full_name||"","text",false,"required")}${isExisting?"":field("임시 비밀번호 · 8자리 이상","password","","text",true,"minlength=\"8\" required")}${roleField}${stateField}<section class="permission-box"><div class="permission-head"><div><strong>사용할 메뉴 선택</strong><p><b id="permissionRoleName">${esc(roleLabel[role])}</b> 추천 메뉴가 표시되어 있습니다. 필요한 메뉴만 체크하세요.</p></div><button class="btn purple" data-action="permission-recommend" type="button">추천으로 되돌리기</button></div><div class="permission-grid">${permissionOptions(role,selected,isSelf)}</div><div class="permission-tools"><button class="mini" data-action="permission-all" type="button">사용 가능 메뉴 전체 선택</button><button class="mini" data-action="permission-none" type="button">전체 해제</button></div><p class="permission-hint">${isSelf?"현재 화면에 다시 들어올 수 있도록 ‘권한·감사’ 메뉴는 항상 유지됩니다.":"체크한 메뉴만 해당 관리자 화면 상단에 표시됩니다. ‘권한·감사’는 최고 관리자에게만 허용됩니다."}</p></section></div><div id="formMessage" class="message"></div><div class="form-actions"><button class="btn" data-action="modal-close" type="button">취소</button><button class="btn primary" type="submit">${isSelf?"내 계정 저장":"저장"}</button></div></form>`,true)
    }
    async function saveStore(form){const fd=Object.fromEntries(new FormData(form)),driver=data.profiles.find(x=>x.id===fd.assigned_driver_id),row={store_code:fd.store_code.trim(),name:fd.name.trim(),address:fd.address.trim(),phone:fd.phone.trim(),contact_name:fd.contact_name.trim(),assigned_driver_id:fd.assigned_driver_id||null,assigned_vehicle:driver?.vehicle_number||null,delivery_order:fd.delivery_order?Number(fd.delivery_order):null,preferred_time:fd.preferred_time.trim(),entry_method:fd.entry_method.trim(),placement:fd.placement.trim(),prohibited:fd.prohibited.trim(),parking_note:fd.parking_note.trim(),handover_note:fd.handover_note.trim(),active:fd.active==="true",updated_by:me.id,updated_at:new Date().toISOString()};let r=form.dataset.id?await client.from("stores").update(row).eq("id",form.dataset.id):await client.from("stores").insert(row);if(r.error)throw r.error}
    function exportExcel(){
      if(!window.XLSX)return toast("Excel 기능을 불러오지 못했습니다.",true);
      const wb=XLSX.utils.book_new(),add=(name,rows)=>XLSX.utils.book_append_sheet(wb,XLSX.utils.json_to_sheet(rows.length?rows:[{"내용":"자료 없음"}]),name);
      add("거래명세",operationalAccountingRows().map(x=>({날짜:workDate,명세번호:x.invoice_no,차수:x.revision_no||"",상태:x.status==="unissued"?"배송자료·미발행":x.status,거래처:x.store_name,호차:x.vehicle_number,배송원:x.driver,배송품목:x.itemSummary,품목금액:x.subtotal,필렛작업비:x.fillet_fee,전미수:x.previous_balance,수금:x.payment_received,남은미수:x.remaining_balance,수정사유:x.invoice?.revision_reason||"",회계메모:x.detail?.accountingNote||""})));
      add("호차별 전체완료",workDrivers().map(d=>{const s=statusOf(d.id),c=completionOf(d.id);return{호차:d.vehicle_number,배송원:d.full_name,전체배송상태:c?(c.result==="issue"?"완료·관리자 확인 필요":"문제 없이 완료"):stateLabel(s.work_state),전체완료시간:c?.at||"",당일배정점포:datedDeliveries(d.id,workDate,false).length,완료메모:c?.note||"",긴급미처리:c?.urgentPending??s.urgent_pending,미해결오류:c?.issueCount??s.issue_count,마지막동기화:c?.lastSyncAt||s.last_sync_at||"",수조전체:s.tank_total,수조상차:s.tank_loaded,박스전체:s.box_total,박스확인:s.box_checked}}));
      const itemRows=[];data.snapshots.forEach(n=>(n.payload?.deliveries||[]).filter(d=>dateKey(d.deliveryDate)===dateKey(workDate)&&d.status!=="cancelled").forEach(d=>(d.items||[]).forEach(i=>itemRows.push({호차:data.profiles.find(p=>p.id===n.driver_id)?.vehicle_number,배송순서:d.routeOrder,거래처:(n.payload.customers||[]).find(c=>c.id===d.customerId)?.name||d.fallbackStoreName,품목:i.species,구분:i.category,원산지:i.originCountry,단위:i.unit,요청수량:i.requestedQty,실계근kg:(i.weighings||[]).reduce((s,w)=>s+Number(w.weight||0),0),단가:i.unitPrice,필렛작업비:i.filletFee,밴드색:i.rubberBandColor,밴드수량:i.rubberBandQuantity,수조:i.tankNumber}))));
      add("품목별 내역",itemRows);
      add("전미수·수금",data.receivables.map(x=>({날짜:x.work_date,거래처:x.store_name,전미수:x.previous_balance,수금:x.payment_received,남은미수:x.remaining_balance,메모:x.note})));
      add("취소·수정 이력",data.invoices.filter(x=>x.status==="cancelled"||x.revision_no>1).map(x=>({명세번호:x.invoice_no,차수:x.revision_no,상태:x.status,거래처:x.store_name,사유:x.revision_reason,시간:x.updated_at})));
      add("긴급 발주",data.urgent.map(x=>({날짜:x.work_date,호차:x.assigned_vehicle,거래처:x.store_name,품목:x.item_summary,수량:x.quantity_summary,상태:x.status,등록:x.created_at,확인:x.acknowledged_at,상차:x.loaded_at,납품:x.delivered_at})));
      add("오류 보고",data.incidents.map(x=>({날짜:x.work_date,호차:x.vehicle_number,유형:x.incident_type,중요도:x.severity,내용:x.description,조치:x.resolution,상태:x.status})));
      XLSX.writeFile(wb,`장백수산_통합업무_${workDate}.xlsx`);toast("당일 통합 Excel을 저장했습니다.");
    }
    function printStore(s){const w=open("","_blank","width=850,height=900");if(!w)return toast("팝업 차단을 해제해 주세요.",true);w.document.write(`<!doctype html><html lang="ko"><head><meta charset="utf-8"><title>${esc(s.name)} 인수인계</title><style>body{font-family:sans-serif;padding:32px;color:#17313d}h1{border-bottom:3px solid #07877c;padding-bottom:14px}.grid{display:grid;grid-template-columns:150px 1fr;border:1px solid #bbb}.grid>*{padding:12px;border-bottom:1px solid #ddd}.grid b{background:#f1f6f7}@media print{button{display:none}}</style></head><body><h1>${esc(s.name)} 배송 인수인계표</h1><p>${s.assigned_vehicle||"-"}호차 · ${s.delivery_order||"-"}번째 배송</p><div class="grid"><b>주소</b><span>${esc(s.address)}</span><b>연락처</b><span>${esc(s.phone)} · ${esc(s.contact_name)}</span><b>입고 시간</b><span>${esc(s.preferred_time)}</span><b>진입 방법</b><span>${esc(s.entry_method)}</span><b>물건 놓는 자리</b><span>${esc(s.placement)}</span><b>금지 사항</b><span>${esc(s.prohibited)}</span><b>주차·하차</b><span>${esc(s.parking_note)}</span><b>기타 인계</b><span>${esc(s.handover_note)}</span></div><p>출입 비밀번호는 보안상 관리자 화면에서 별도로 확인합니다.</p><button onclick="print()">인쇄</button></body></html>`);w.document.close()}
    document.addEventListener("click",async e=>{const b=e.target.closest("button");if(!b)return;const a=b.dataset.action,id=b.dataset.id;if(b.dataset.view){view=b.dataset.view;search="";renderTabs();render();return}if(a==="top-refresh")return forceRefresh(b);if(a==="modal-close")return $("#modal").classList.add("hidden");if(a==="permission-recommend")return syncPermissionForm(true);if(a==="permission-all"){const f=$("#staffForm");if(f)f.querySelectorAll('input[name="menuPermissions"]:not(:disabled)').forEach(input=>input.checked=true);return}if(a==="permission-none"){const f=$("#staffForm");if(f)f.querySelectorAll('input[name="menuPermissions"]:not(:disabled)').forEach(input=>input.checked=false);return}
      try{
        if(a==="driver-new")return driverForm();if(a==="driver-edit")return driverForm(drivers().find(x=>x.id===id));if(a==="driver-delete"){const target=drivers().find(x=>x.id===id);if(!target)return toast("삭제할 배송원을 찾지 못했습니다.",true);if(!confirm(`${target.full_name} 배송원 계정을 완전히 삭제할까요?\n\n로그인 계정과 중앙에 저장된 해당 배송원 자료가 삭제되며 되돌릴 수 없습니다.\n기록 보존이 필요하면 삭제 대신 사용 정지를 선택하세요.`))return;const typed=prompt(`오삭제 방지를 위해 배송원 아이디를 입력하세요.\n\n${target.employee_code}`,"");if(typed!==target.employee_code)return toast("아이디가 일치하지 않아 삭제를 취소했습니다.",true);await api("/api/admin/delete-driver",{driverId:id});toast(`${target.full_name} 배송원 계정을 삭제했습니다.`);return loadAll()}if(a==="store-new")return storeForm();if(a==="store-edit")return storeForm(data.stores.find(x=>x.id===id));if(a==="store-print")return printStore(data.stores.find(x=>x.id===id));if(a==="urgent-new")return urgentForm();if(a==="urgent-edit")return urgentForm(data.urgent.find(x=>String(x.id)===id));if(a==="product-new")return productForm();if(a==="product-edit")return productForm(data.products.find(x=>x.id===id));if(a==="invoice-new")return invoiceForm();if(a==="invoice-source-edit"){const source=operationalAccountingRows().find(row=>row.key===b.dataset.key);return source?invoiceForm(null,source):toast("배송 회계자료를 찾지 못했습니다. 새로고침 후 다시 시도해 주세요.",true)}if(a==="invoice-revise")return invoiceForm(data.invoices.find(x=>x.id===id));if(a==="staff-new")return staffForm();if(a==="staff-edit")return staffForm(data.profiles.find(x=>x.id===id));if(a==="staff-delete"){const target=data.profiles.find(x=>x.id===id);if(!target)return toast("삭제할 관리자를 찾지 못했습니다.",true);if(!confirm(`${target.full_name} 관리자 계정을 삭제할까요?\n\n삭제하면 이 계정으로 다시 로그인할 수 없으며 되돌릴 수 없습니다.`))return;await api("/api/admin/delete-staff",{profileId:id});toast(`${target.full_name} 관리자 계정을 삭제했습니다.`);return loadAll()}if(a==="export-purchases")return exportPurchaseExcel();if(a==="export-shipments")return exportShipmentExcel();if(a==="export-excel")return exportExcel();
        if(a==="purchase-details")return showPurchaseDetails(id);
        if(a==="driver-details")return showDriverWorkdayDetails(id);
        if(a==="workday-edit")return showDriverWorkdayEditor(id);
        if(a==="workday-add-delivery"){const form=$("#workdayEditorForm"),container=$("#workdayDeliveries");if(!form||!container)return;const work=driverWorkday(form.dataset.driverId),index=container.querySelectorAll(".editor-delivery").length,draft={id:safeId(),customerId:0,deliveryDate:workDate,routeOrder:index+1,status:"ready",items:[],boxes:[]};container.insertAdjacentHTML("beforeend",editorDeliveryHtml(work,draft,index));container.lastElementChild?.scrollIntoView({behavior:"smooth",block:"start"});return}
        if(a==="workday-add-item"){const section=b.closest(".editor-delivery"),container=section?.querySelector(".editor-items");if(!container)return;const index=container.querySelectorAll(".editor-item").length;container.insertAdjacentHTML("beforeend",editorItemHtml({},index));container.lastElementChild?.scrollIntoView({behavior:"smooth",block:"center"});return}
        if(a==="workday-add-box"){const section=b.closest(".editor-delivery"),container=section?.querySelector(".editor-boxes");if(!container)return;const index=container.querySelectorAll(".editor-box").length;container.insertAdjacentHTML("beforeend",editorBoxHtml({},index));container.lastElementChild?.scrollIntoView({behavior:"smooth",block:"center"});return}
        if(a==="departure-reset"){const reason=prompt("출차 승인 취소 사유를 입력해 주세요.","상차 내용 재확인");if(!reason?.trim())return toast("승인 취소 사유가 필요합니다.",true);await api("/api/admin/update-driver",{action:"reset_departure",driverId:id,workDate,reason:reason.trim()});toast("출차 승인을 취소하고 감사 기록을 남겼습니다.");await loadAll();return showDriverWorkdayDetails(id)}
        if(a==="driver-history"){const d=drivers().find(x=>x.id===id),h=data.assignments.filter(x=>x.driver_id===id);return modal(`${esc(d.full_name)} 호차 배정 이력`,`퇴사·호차 이동 인수인계 근거입니다.`,`<div class="modal-body"><div class="card-grid" style="grid-template-columns:1fr">${h.map(x=>`<article class="info-card"><h3>${x.vehicle_number}호차 · ${x.active?"현재 배정":"종료"}</h3><p>${x.start_date} ~ ${x.end_date||"현재"}</p><p>${esc(x.note||"")}</p></article>`).join("")||`<div class="empty">이력이 없습니다.</div>`}</div></div>`)}
        if(a==="departure-approve"){const d=drivers().find(x=>x.id===id),s=statusOf(id),c=data.departures.find(x=>x.driver_id===id)||{};if(!(s.work_state==="departure_ready"||c.driver_ready)&&!confirm("배송원의 최종 점검 완료 신호가 없습니다. 그래도 승인할까요?"))return;const row={driver_id:id,work_date:workDate,vehicle_number:d.vehicle_number,orders_checked:true,tanks_checked:s.tank_total===s.tank_loaded,boxes_checked:s.box_total===s.box_checked,urgent_checked:!s.urgent_pending,handover_checked:true,driver_ready:true,approved:true,approved_by:me.id,approved_at:new Date().toISOString(),reject_reason:"",updated_at:new Date().toISOString()};const {error}=await client.from("departure_checks").upsert(row,{onConflict:"driver_id,work_date"});if(error)throw error;toast(`${d.vehicle_number}호차 출차를 승인했습니다.`);return loadAll()}
        if(a==="toggle-close"){const closed=data.workday?.is_closed;if(!confirm(closed?"업무 마감을 해제하면 배송원이 다시 수정할 수 있습니다. 해제할까요?":"모든 호차와 긴급 발주를 확인했습니까? 당일 업무를 마감·잠금할까요?"))return;const reason=prompt(closed?"마감 해제 사유":"마감 메모","당일 업무 확인 완료")||"";const row={work_date:workDate,is_closed:!closed,closed_at:closed?null:new Date().toISOString(),closed_by:closed?null:me.id,close_reason:reason,backup_status:data.snapshots.length?"healthy":"warning",backup_checked_at:new Date().toISOString(),backup_note:`중앙 백업 ${data.snapshots.length}호차 확인`,updated_at:new Date().toISOString()};const {error}=await client.from("workday_controls").upsert(row);if(error)throw error;toast(closed?"업무 마감을 해제했습니다.":"당일 업무를 마감·잠금했습니다.");return loadAll()}
        if(a==="urgent-status"){const x=data.urgent.find(x=>String(x.id)===id),next=prompt("상태를 입력하세요: new / acknowledged / loaded / delivered / impossible / cancelled",x.status);if(!next||!["new","acknowledged","loaded","delivered","impossible","cancelled"].includes(next))return;const row={status:next,updated_at:new Date().toISOString()};if(next==="acknowledged")row.acknowledged_at=new Date().toISOString();if(next==="loaded")row.loaded_at=new Date().toISOString();if(next==="delivered")row.delivered_at=new Date().toISOString();if(next==="impossible")row.impossible_reason=prompt("처리 불가 사유",x.impossible_reason||"")||"";const {error}=await client.from("urgent_orders").update(row).eq("id",id);if(error)throw error;toast("긴급 발주 상태를 변경했습니다.");return loadAll()}
        if(a==="incident-manage"){const x=data.incidents.find(x=>String(x.id)===id),resolution=prompt("조치 내용을 입력하세요.",x.resolution||"");if(resolution===null)return;const status=prompt("상태: reported / checking / resolved / closed",x.status);if(!["reported","checking","resolved","closed"].includes(status))return;const {error}=await client.from("incident_reports").update({resolution,status,updated_at:new Date().toISOString()}).eq("id",id);if(error)throw error;toast("오류 조치 기록을 저장했습니다.");return loadAll()}
        if(a==="store-secret"){const x=data.stores.find(x=>x.id===id),r=await api("/api/admin/store-secret",{action:"get",storeId:id}),next=prompt(`${x.name} 출입 비밀번호·출입코드\n열람 기록이 남습니다. 수정 후 확인을 누르세요.`,r.value||"");if(next===null)return;await api("/api/admin/store-secret",{action:"set",storeId:id,value:next});return toast("보안 출입정보를 저장했습니다.")}
        if(a==="password-reset"){const p=prompt("새 임시 비밀번호를 8자리 이상 입력하세요.");if(!p)return;await api("/api/admin/reset-password",{driverId:$("#driverForm").dataset.id,password:p});return toast("임시 비밀번호를 변경했습니다.")}
        if(a==="invoice-cancel"){const x=data.invoices.find(x=>x.id===id),reason=prompt("취소 사유를 입력하세요.");if(!reason)return;const {error}=await client.from("invoice_records").update({status:"cancelled",revision_reason:reason,updated_at:new Date().toISOString()}).eq("id",id);if(error)throw error;toast("거래명세 취소 이력을 보존했습니다.");return loadAll()}
        if(a==="price-new"){const p=data.products.find(x=>x.id===id),sid=prompt(`거래처별 단가를 등록합니다.\n거래처 코드 또는 이름을 입력하세요.`),store=data.stores.find(s=>s.store_code===sid||s.name===sid);if(!store)return toast("거래처를 찾지 못했습니다.",true);const price=Number(prompt(`${store.name}의 ${p.name} 단가`,p.base_price));if(!Number.isFinite(price)||price<0)return;const {error}=await client.from("product_prices").insert({product_id:p.id,store_id:store.id,unit_price:price,fillet_fee_per_kg:p.fillet_fee_per_kg,effective_from:workDate,created_by:me.id});if(error)throw error;toast("거래처별 단가를 저장했습니다.");return loadAll()}
      }catch(err){toast(err.message||"처리하지 못했습니다.",true)}});
    document.addEventListener("input",e=>{if(e.target.id==="searchInput"){search=e.target.value;render()}});document.addEventListener("change",e=>{if(e.target.id==="workDate"){workDate=e.target.value;loadAll()}if(e.target.id==="shipmentVehicleFilter"){shipmentVehicle=e.target.value;render()}if(e.target.id==="shipmentStatusFilter"){shipmentStatus=e.target.value;render()}if(e.target.closest("#staffForm")&&e.target.name==="role")syncPermissionForm(true);if(e.target.closest("#urgentForm")&&e.target.name==="store_id"){const o=e.target.selectedOptions[0];e.target.form.assigned_vehicle.value=o?.dataset.vehicle||""}if(e.target.closest("#invoiceForm")&&e.target.name==="store_id"){const o=e.target.selectedOptions[0];if(o?.value){e.target.form.vehicle_number.value=o.dataset.vehicle||e.target.form.vehicle_number.value;e.target.form.store_name.value=o.dataset.name||e.target.form.store_name.value}}});
    document.addEventListener("submit",async e=>{e.preventDefault();const f=e.target,m=f.querySelector("#formMessage"),formData=new FormData(f),fd=Object.fromEntries(formData);if(m)m.textContent="저장 중입니다.";try{
      if(f.id==="loginForm"){const {data:s,error}=await client.auth.signInWithPassword({email:fd.email.trim(),password:fd.password});if(error)throw error;await accept(s.session);return}
      if(f.id==="workdayEditorForm"){const payload=collectDriverWorkday(f);if(!payload.reason)throw new Error("수정 사유를 반드시 입력해 주세요.");await api("/api/admin/update-driver",{action:"update_workday",...payload});$("#modal").classList.add("hidden");toast("당일 배송·상차 내용을 저장하고 감사 기록을 남겼습니다.");await loadAll();return showDriverWorkdayDetails(payload.driverId)}
      if(f.id==="driverForm"){if(f.dataset.id)await api("/api/admin/update-driver",{driverId:f.dataset.id,employeeCode:fd.employeeCode,fullName:fd.fullName,phone:fd.phone,vehicleNumber:Number(fd.vehicleNumber),active:fd.active==="true",note:fd.note});else await api("/api/admin/create-driver",fd)}
      if(f.id==="storeForm")await saveStore(f);
      if(f.id==="urgentForm"){const o=f.store_id.selectedOptions[0],store=data.stores.find(s=>s.id===fd.store_id),row={work_date:workDate,store_id:fd.store_id,store_name:o?.dataset.name||store?.name||"",assigned_driver_id:store?.assigned_driver_id||null,assigned_vehicle:Number(fd.assigned_vehicle),item_summary:fd.item_summary.trim(),quantity_summary:fd.quantity_summary.trim(),note:fd.note.trim(),priority:fd.priority,updated_at:new Date().toISOString()};const r=f.dataset.id?await client.from("urgent_orders").update(row).eq("id",f.dataset.id):await client.from("urgent_orders").insert({...row,status:"new",created_by:me.id});if(r.error)throw r.error}
      if(f.id==="productForm"){const row={product_code:fd.product_code.trim(),name:fd.name.trim(),category:fd.category,unit:fd.unit,origin_country:fd.origin_country.trim(),base_price:Number(fd.base_price),fillet_fee_per_kg:Number(fd.fillet_fee_per_kg||0),default_band_color:fd.default_band_color.trim(),effective_from:fd.effective_from,effective_to:fd.effective_to||null,active:fd.active==="true",updated_by:me.id,updated_at:new Date().toISOString()};const r=f.dataset.id?await client.from("products").update(row).eq("id",f.dataset.id):await client.from("products").insert(row);if(r.error)throw r.error}
      if(f.id==="invoiceForm"){
        const old=data.invoices.find(x=>x.id===f.dataset.id),store=data.stores.find(s=>s.id===fd.store_id),subtotal=Number(fd.subtotal||0),fee=Number(fd.fillet_fee||0),prev=Number(fd.previous_balance||0),received=Number(fd.payment_received||0),invoiceNo=fd.invoice_no.trim(),same=data.invoices.filter(x=>x.invoice_no===invoiceNo),maxRevision=same.reduce((max,row)=>Math.max(max,Number(row.revision_no)||0),0),revisionNo=maxRevision+1,storeName=String(fd.store_name||store?.name||"").trim(),detail={...(old?.detail||{}),sourceDeliveryId:fd.source_delivery_id||old?.detail?.sourceDeliveryId||null,sourceDriverId:fd.driver_id||old?.driver_id||old?.detail?.sourceDriverId||null,itemSummary:fd.item_summary||old?.detail?.itemSummary||"",accountingNote:String(fd.accounting_note||"").trim()};
        if(!storeName)throw new Error("거래처명을 입력해 주세요.");
        const row={work_date:workDate,invoice_no:invoiceNo,driver_id:fd.driver_id||old?.driver_id||null,store_id:fd.store_id||old?.store_id||null,store_name:storeName,vehicle_number:Number(fd.vehicle_number)||store?.assigned_vehicle||null,subtotal,fillet_fee:fee,previous_balance:prev,payment_received:received,remaining_balance:prev+subtotal+fee-received,status:old?"revised":"issued",revision_no:revisionNo,replaces_invoice_id:old?.id||null,detail,revision_reason:String(fd.revision_reason||"").trim(),created_by:me.id};
        const {error}=await client.from("invoice_records").insert(row);if(error)throw error;
        const {error:receivableError}=await client.from("receivable_payments").insert({work_date:workDate,store_id:row.store_id,store_name:storeName,previous_balance:prev,payment_received:received,remaining_balance:row.remaining_balance,note:`명세 ${row.invoice_no} ${old?`수정 ${row.revision_no}차`:"회계 확정"}${detail.accountingNote?` · ${detail.accountingNote}`:""}`,created_by:me.id});if(receivableError)throw receivableError
      }
      if(f.id==="staffForm"){const isSelf=f.dataset.self==="true",menuPermissions=formData.getAll("menuPermissions");if(isSelf&&!menuPermissions.includes("access"))menuPermissions.push("access");if(!menuPermissions.length)throw new Error("사용할 메뉴를 1개 이상 선택해 주세요.");if(isSelf){const result=await api("/api/admin/update-self",{employeeCode:fd.employeeCode,fullName:fd.fullName,menuPermissions});me=result.profile;$("#adminName").textContent=me.full_name;$("#adminRole").textContent=roleLabel[me.role]}else if(f.dataset.id)await api("/api/admin/update-role",{profileId:f.dataset.id,employeeCode:fd.employeeCode,fullName:fd.fullName,role:fd.role,active:fd.active==="true",menuPermissions});else await api("/api/admin/create-staff",{...fd,menuPermissions})}
      $("#modal").classList.add("hidden");toast("저장했습니다.");await loadAll()
    }catch(err){if(m)m.textContent=err.message||"저장하지 못했습니다.";else toast(err.message,true)}});
    $("#loginForm").addEventListener("submit",()=>{});$("#logoutButton").addEventListener("click",async()=>{clearInterval(poll);await client.auth.signOut();location.reload()});boot();
  })();
  </script>
</body>
</html>
